---
phase: 02-export-for-claude
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - index.html
  - test/harness.js
  - test/app.test.js
autonomous: false
requirements: [EXP-01, EXP-02, EXP-04, EXP-06]

estimate:
  tokens: 70000
  raw_tokens: 70000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-02: Settings → Backup shows an 'Export for Claude (.md)' button with the arrow-square-out Phosphor icon, directly below 'Export backup (.json)' and above 'Import backup'; index.html has exactly one onclick that calls exportMarkdown(), so nothing is added to the home backup-reminder card"
    - "D-03 / EXP-01: tapping the button downloads a Markdown file named ppl-export-YYYY-MM-DD.md (from todayISO()) with type text/markdown, and its text is exactly buildMarkdownExport(); the JSON backup still downloads ppl-backup-YYYY-MM-DD.json and still records lastBackupAt"
    - "D-08: every COLLECTIONS column is a {field, label, unit?} object, the single primary column representation with no parallel label map; the unit kind 'mass' resolves to DB.unit when the export runs, and any other unit string (cardio's 'km') is used literally"
    - "D-10: headers are readable labels: Workouts reads date | workout | exercise | set | weight (lb) | reps; Weigh-ins and Pet weigh-ins read date | weight (lb); Cardio reads date | type | minutes | distance (km) | note; Hobbies & chores reads date | item | category"
    - "D-12 / EXP-02 ordering: sections appear in Object.keys(COLLECTIONS) order, one '## <label>' heading per collection, with no grouping metadata; a probe collection inserted first by source transform appears first"
    - "D-06 / EXP-02 empty: a collection with no live rows still gets its heading followed by the line 'No entries'; a fresh install exports all eleven sections that way without throwing"
    - "D-09: a null, undefined, empty or whitespace-only cell is written as — (em dash), never as an empty cell and never as 0"
    - "EXP-02: a probe list and a probe map added to COLLECTIONS by source transform each export their own section (the probe's mass column labelled from DB.unit, its soft-deleted row absent) with no exporter line changed; and no exporter function names a collection, a unit literal, a named live wrapper, fmtDate, kmToDisp or esc()"
    - "EXP-02 adjacency: mobilityLog and lawnLog share the dayFlagRows format but export as two separate sections, each with its own heading; collectionProblems refuses two collections declaring the same label and names both"
    - "EXP-04: list collections are read only through liveOf(name) and map collections through DB[name], which is what the app's own views do; the soft-deleted LEGS 2 session in the smoke fixture is absent from the file"
    - "EXP-05 (declaration side): collectionProblems refuses a column whose field is id, mtime or deletedAt, so an internal id cannot be declared into the export"
    - "collectionProblems validates the new shape: columns is non-empty; each column is a plain object holding only field, label and unit; field and label are non-empty strings; labels and units hold no | and no line break; fields and labels are distinct within a collection; every collection declares a label that holds no | or line break and is distinct across the registry"
    - "EXP-06 / EXP-07 (tracer level): every cell is trimmed, has line-break runs replaced by <br>, and has each | escaped with the backslash run before it doubled, so every table row in the smoke-fixture export has exactly its header's cell count"
    - "No Phase 1 check regresses: the SLEEP-05 probe lines, the refusal-battery factories, the duplicate-columns refusal and the REG-17 rows test move to the new column shape in the same commit as the registry change, and the suite ends 0 failed"
  artifacts:
    - path: "index.html"
      provides: "column-object COLLECTIONS with labels, collectionProblems shape rules, mdEscape, mdCell, mdHeader, exportRows, buildMarkdownExport, downloadMarkdown, exportMarkdown, the Settings button"
      contains: "function buildMarkdownExport("
    - path: "test/harness.js"
      provides: "exports for the seven exporter functions"
      contains: "'buildMarkdownExport'"
    - path: "test/app.test.js"
      provides: "export tracer tests, the mdCells/mdSections helpers, new-shape registry refusals, EXP-02 probe and static proofs"
      contains: "── export for Claude"
  key_links:
    - from: "Settings → Backup card in viewData()"
      to: "exportMarkdown()"
      via: "inline onclick directly below the JSON export button"
      pattern: "onclick=\"exportMarkdown\\(\\)\""
    - from: "buildMarkdownExport()"
      to: "COLLECTIONS"
      via: "Object.keys(COLLECTIONS) in declared order, reading label, columns and format"
      pattern: "Object\\.keys\\(COLLECTIONS\\)"
    - from: "exportRows(name)"
      to: "liveOf(name)"
      via: "the soft-delete read path for every list collection"
      pattern: "liveOf\\(name\\)"
    - from: "mdHeader(col)"
      to: "DB.unit"
      via: "unit kind 'mass' resolved at export time"
      pattern: "DB\\.unit"
  prohibitions:
    - statement: "MUST NOT write to DB or localStorage anywhere on the export path: no save(), saveLocal() or touch(), no lastBackupAt or backupSnoozeAt. save() bumps updatedAt, and a device that looks newest just because Ian exported from it is the stale-device sync incident CLAUDE.md records"
      requirement_id: EXP-01
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT transmit the export on its own: no fetch, no Firestore write, no cloud push. The file leaves the app only as a download or through a share sheet that Ian opens and aims himself"
      requirement_id: EXP-02
      category: privacy
      status: resolved
      verification: test
---

<objective>
Lead the phase with the tracer slice. Promote every `COLLECTIONS` column to a `{field,label,unit}` object and give every collection a `label`. Build `buildMarkdownExport()` generically from the registry and the live read path, and put an "Export for Claude (.md)" button in Settings that downloads the result. Task 2 then hardens the registry contract for the new shape and proves EXP-02: a twelfth collection exports with no exporter edit.

Purpose: the one architectural decision left open by REG-17 is the column metadata shape (D-08). Every later plan, the Phase 3 recipe and every future collection inherit it. This plan settles it end to end, from the registry literal through the validator, the exporter and the button to a downloaded file, before any content rules land on top.

Output: the new column shape on all eleven collections, `collectionProblems()` shape rules, the seven exporter functions, the Settings button, harness exports and the export test section.

**Designed human stop (surfaced up front, per Observation 3):** Task 1 is `type="tracer"`. In an interactive run (`workflow.auto_advance` is false in `.planning/config.json`), the executor stops once, right after Task 1's commit, and returns a `checkpoint:human-verify` for the tracer before starting Task 2. That is why this plan is `autonomous: false`. The check is the tracer's `<verify>` plus its `<human-check>`. No other stop is designed into this plan.

Constraints on every task: keep the suite green at every commit. Do not push to `main`, because CI deploys `main` to Ian's phone. Code reaches `main` through a PR after the phase.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-export-for-claude/02-CONTEXT.md
@.planning/phases/02-export-for-claude/02-RESEARCH.md
@.planning/phases/02-export-for-claude/02-PATTERNS.md
@CLAUDE.md
@index.html
@test/harness.js
@test/app.test.js

<interfaces>
Existing, used as-is (current line numbers on this branch; re-locate by name if they drift):
- `const COLLECTIONS = {` index.html:489-505, eleven one-line entries: sessions, weights, petWeights, cardio, ideas, todos, hobbyLog, journal, mobilityLog, lawnLog, sleep. The module-eval validation block `COLLECTIONS is invalid: ` is at 506-509.
- `function liveOf(name)` index.html:712. It throws for any name that is not `kind:'list', soft:true`, so it must never be called for the three map collections.
- `function sessionRows(s)`, `hobbyRows(h)`, `journalRows(date, text)`, `dayFlagRows(date, obj)` index.html:3797-3818. They return raw row objects keyed by column field.
- `function collectionProblems(reg)` index.html:3822-3857. `ALLOWED` is at 3827 and the columns branch at 3850-3852. Every problem string starts with `name + ': '`.
- `function viewData()` index.html:3307. The Backup card is at 3348-3354, and the JSON export button at 3351.
- `function exportData()` index.html:3361-3366 holds the Blob + anchor download pattern.
- `todayISO()` index.html:814. `ph(name)` is the inline Phosphor icon helper; `'arrow-square-out'` exists in the PH map.
- test/harness.js:101-125 is the `names` array, where an unlisted name comes back undefined. The sandbox's `Blob`, `document.createElement` and `navigator` are plain properties of `__sandbox` and can be overridden per instance.
- test/app.test.js: `ok(name, cond, extra)` at 19, `REQUIRED_EXPORTS` at 121-127, the SLEEP-05 `PROBE_LIST_LINE`/`PROBE_MAP_LINE`/`probe` at 1500-1503, the refusal battery with `validListSpec`/`validMapSpec` at 1777-1778, the duplicate-columns refusal at 1837-1839, the REG-17 rows keys test at 1895-1897, `populatedDB(a)` (hoisted) at 2053, `draftFor` at 2119, and the REG-01 "never mutated" block at 2188.

Created by this plan:
- Column spec: `{ field: string, label: string, unit?: string }`. `unit` is `'mass'` (resolved to DB.unit) or a literal unit string.
- Collection field `label: string`, which is required, distinct and used as the section heading.
- `mdEscape(s)` → string. `mdCell(v)` → string. `mdHeader(col)` → string.
- `exportRows(name)` → an array of row objects holding exactly the declared column fields, in column order.
- `buildMarkdownExport()` → string. It takes no arguments, reads DB and COLLECTIONS, and writes nothing.
- `downloadMarkdown(text, filename)` → true. `exportMarkdown()` → `'download'`.
- test/app.test.js helpers `mdCells(line)` and `mdSections(md)`, and the constant `EXPORTER_FNS`.
</interfaces>
</context>

<assumption_delta_decision>
- Primary noun: the COLLECTIONS column spec (`COLLECTIONS[*].columns`).
- Decision: **promote**. `columns` changes from `string[]` to `{field,label,unit}[]` in place and becomes the single primary representation. No parallel label/unit map is added next to a surviving string list, because two lists would be the "second hand-maintained schema" the phase goal forbids, and an array keeps the column order the Phase 1 contract already relies on.
- Secondary noun: the export artifact. Decision: **add-alongside**, as EXP-01 requires. The JSON backup is untouched and stays the only round-trip format (REQUIREMENTS Out of Scope). The Markdown file is a derived, read-only view.
- Invariant test: Task 2's probe collection, injected by source transform, exports its own section with a DB.unit-resolved header and no exporter edit. A static check also asserts that no exporter function names a collection or a unit.
</assumption_delta_decision>

<tasks>

<task type="tracer">
  <name>Task 1: Tracer — promote columns to {field,label,unit}, build the Markdown export from COLLECTIONS, download it from a Settings button</name>
  <reversibility rating="costly">The Phase 3 recipe documents this column shape and every future collection declares it; changing it again means editing all eleven entries, collectionProblems and the exporter together. No stored data depends on it, so it is not one-way.</reversibility>
  <files>index.html, test/harness.js, test/app.test.js</files>
  <read_first>
    - index.html lines 479-509 (the COLLECTIONS comment block, literal and module-eval validation)
    - index.html lines 704-731 (isLive, liveOf and the live wrappers)
    - index.html lines 3793-3857 (the REG-17 row shapers and collectionProblems)
    - index.html lines 3348-3366 (the Settings Backup card and exportData)
    - test/harness.js lines 69-133 (sandbox stubs, names array, api object)
    - test/app.test.js lines 115-143, 1494-1513, 1775-1840, 1884-1912, 2051-2130, 2180-2209
    - .planning/phases/02-export-for-claude/02-CONTEXT.md (D-01 to D-13, all of it)
    - .planning/phases/02-export-for-claude/02-RESEARCH.md sections "Architecture Patterns", "Common Pitfalls" (1, 5, 6) and "Code Examples"
    - CLAUDE.md (whole file; it is authoritative)
  </read_first>
  <action>
Implements D-02, D-03, D-06, D-08, D-09, D-10, D-12 and the tracer path of EXP-01, EXP-02, EXP-04, EXP-06 and EXP-07.

1. COLLECTIONS literal (index.html ~489-505). Give every entry a `label`, and replace every `columns` string array with column objects (per D-08, promoting in place, with no parallel map). Keep each entry on one line. Keep the field order kind, key, sortBy, merge, soft, required, explicitFalse, then `label`, `columns`, `format`, so merge still precedes any closing brace, which the Phase 1 refusal fixture's regex relies on. Only the columns listed with a unit get one. Exact values, written as field→label:
   - sessions: label 'Workouts'. Columns: date→date, workout→workout, exercise→exercise, set→set, weight→weight with unit 'mass', reps→reps.
   - weights: 'Weigh-ins'. date→date, value→weight with unit 'mass'.
   - petWeights: 'Pet weigh-ins'. date→date, value→weight with unit 'mass'.
   - cardio: 'Cardio'. date→date, type→type, minutes→minutes, distanceKm→distance with unit 'km', note→note.
   - ideas: 'Ideas'. date→date, text→idea, done→done.
   - todos: 'Todos'. created→created, text→todo.
   - hobbyLog: 'Hobbies & chores'. date→date, item→item, cat→category.
   - journal: 'Journal'. date→date, entry→entry.
   - mobilityLog: 'Mobility'. date→date, item→stretch, done→done.
   - lawnLog: 'Lawn'. date→date, item→task, done→done.
   - sleep: 'Sleep'. date→date, hours→hours, quality→'quality (1-5)', note→note.
   In the comment block above the literal, add `label` to the list of fields read only after boot. Add two sentences on the column contract. First: `columns[0]` is always the row's ISO date, which the export sorts by and builds its date range from. Second: a column's `unit` is either the kind 'mass', resolved to DB.unit when the export runs, or a literal unit string such as 'km'.

2. collectionProblems (index.html ~3822). Add 'label' to `ALLOWED`. Add a label rule: `label` must be a string that is non-empty after trim and holds no `|` and no line break (\r, \n, U+2028, U+2029); otherwise push "label must be a non-empty string with no | or line break". Replace the columns branch with these rules, substituting the column's index for i in each message:
   - not an array, or empty → "columns must be a non-empty array of column objects"
   - an item that is not a non-null, non-array object → "columns[i] must be an object"
   - any key other than field, label or unit → 'columns[i] has unknown key "k"'
   - field not a non-empty string → "columns[i].field must be a non-empty string"
   - field equal to id, mtime or deletedAt → 'columns[i].field "X" is internal and must never be exported' (EXP-05)
   - label that fails the label rule → "columns[i].label must be a non-empty string with no | or line break"
   - a present `unit` that fails the same rule → "columns[i].unit must be a non-empty string with no | or line break"
   - a repeated field → "columns must not repeat a field"; a repeated label → "columns must not repeat a label"
   After the per-entry loop, add a registry-level pass over entries whose label is a string. For every label used by more than one collection, push one problem per collection that uses it, of the form name + ': label "L" is also used by ' + the other names joined with ', '. Keep the format rules unchanged. Every problem still starts with the collection name and a colon.

3. Presentation helpers. Add these as function declarations directly after `dayFlagRows()` and before the collectionProblems comment, under a comment header "── Markdown export presentation (Phase 2) ──". The comment says three things: never esc(), because HTML entities corrupt plain text in a file Claude reads as Markdown (RESEARCH Pitfall 1); dates pass through as stored ISO strings and never through fmtDate; the em dash is the one missing marker (D-09).
   - `mdEscape(s)`: take String(s) and trim it with String.prototype.trim. Replace every run of whitespace that contains a line break (\r\n, \r, \n, U+2028 or U+2029) with the four characters `<br>`. Then replace every `|`, together with the run of zero or more backslashes directly before it, with that backslash run doubled followed by a backslash and the pipe. A pipe then always follows an odd number of backslashes, so a GFM table parser can never read it as a cell boundary. Return the result.
   - `mdCell(v)`: null or undefined → '—'. A non-null object → JSON.stringify(v). Anything else → String(v). Run the result through mdEscape, and if it is empty return '—'.
   - `mdHeader(col)`: the unit is DB.unit when col.unit is 'mass', and col.unit otherwise. When that unit is a string that is non-empty after trim, return mdEscape of label + ' (' + trimmed unit + ')'. Otherwise return mdEscape(label).

4. Export functions. Add these as function declarations directly after `exportData()`, under a comment that calls this "the other export". It is derived from COLLECTIONS and the same live read path the views use. It never persists: no save/saveLocal/touch, and no lastBackupAt, because a Markdown file is not a backup and exporting must never make this device look newest to sync.
   - `exportRows(name)`: let spec = COLLECTIONS[name].
     - For kind 'list', the source is `liveOf(name)` (EXP-04). Each source item that is a non-null object yields spec.format(item) when format is a function, and [item] otherwise.
     - For kind 'map', obj is DB[name], or {} when that is not a non-null, non-array object. For each key of Object.keys(obj).sort(), the output is spec.format(key, obj[key]).
     - A non-array format result contributes nothing.
     - Project every output item that is a non-null object into a new object that holds exactly spec.columns' fields, in column order, copied from that item (EXP-05's structural guarantee: nothing undeclared can reach the file).
     - Return the projected rows.
   - `buildMarkdownExport()`: start the lines with '# PPL Tracker export', '', 'Logged data from PPL Tracker, one Markdown table per collection. Deleted entries and internal ids are not included, and — marks a missing value.', '', '- Generated: ' + new Date().toISOString(). For each name of Object.keys(COLLECTIONS) in order (D-12), take rows = exportRows(name) and push '', '## ' + mdEscape(spec.label), ''.
     - When rows is empty, push the line 'No entries' (D-06).
     - Otherwise push a header line: '| ' + the columns mapped through mdHeader and joined with ' | ' + ' |'.
     - Then a delimiter line of '---' per column in the same pipe layout.
     - Then one line per row, of mdCell(row[col.field]) per column in the same pipe layout.
     Return lines.join('\n') + '\n'. It takes no arguments and reads only DB and COLLECTIONS.
   - `downloadMarkdown(text, filename)`: exportData()'s Blob + anchor pattern with type 'text/markdown'. That is new Blob([text], {type:'text/markdown'}), an anchor from document.createElement('a'), href from URL.createObjectURL, download set to filename, then click(). Return true. Do not copy exportData's last line (lastBackupAt, backupSnoozeAt, save).
   - `exportMarkdown()`: text = buildMarkdownExport(); filename = 'ppl-export-' + todayISO() + '.md' (D-03); call downloadMarkdown(text, filename); return 'download'. This task ships the download path. The share-sheet branch in front of it (D-01) belongs to plan 02-03.

5. viewData (D-02). In the Backup card, directly after the "Export backup (.json)" button and before the "Import backup" button, add a button with class "btn btn-ghost btn-block", style margin-top:10px (the same spacing the Import button uses) and onclick exportMarkdown(). Its content is ph('arrow-square-out') followed by the text "Export for Claude (.md)". Use no hard-coded colours; the existing classes carry the Nocturne theme.

6. test/harness.js: append 'mdEscape', 'mdCell', 'mdHeader', 'exportRows', 'buildMarkdownExport', 'exportMarkdown' and 'downloadMarkdown' to `names`.

7. test/app.test.js, existing Phase 1 checks moved to the new shape in this same commit (RESEARCH Pitfall 5):
   - `PROBE_LIST_LINE`: add label 'Probe list'. Columns become date→date and value→value with unit 'mass'.
   - `PROBE_MAP_LINE`: add label 'Probe map'. Columns become date→date, item→item and done→done.
   - `validListSpec`: add label 'Thing'; columns become one column, date→date.
   - `validMapSpec`: add label 'Thing'; columns become date→date and entry→entry.
   - The duplicate-columns refusal uses two identical date→date column objects.
   - The REG-17 rows ok compares each row's Object.keys with `app.COLLECTIONS.sessions.columns.map(c=>c.field)`. Its label becomes "rows: every row's keys equal the COLLECTIONS.sessions column fields, in order".
   - Append the seven names from step 6 to `REQUIRED_EXPORTS`.

8. test/app.test.js, new section. Insert it immediately before the REG-01 "never mutated" comment block near the end of the file. Its console header is "── export for Claude (EXP-01…EXP-08) ──". It defines two helpers and `EXPORTER_FNS`:
   - `mdCells(line)` splits one table line the way a GFM parser does. Walk the characters: a backslash consumes itself and the next character into the current cell, and an unconsumed `|` ends the cell. Drop the empty text before the first pipe and after the last one, and trim each cell. It returns the raw cell texts with escapes kept.
   - `mdSections(md)` maps each '## ' heading text to an object `{ empty, header, rows }`. `empty` is true when the first non-blank line after the heading is 'No entries'. `header` is the mdCells of the first table line. `rows` is the mdCells of each table line after the delimiter line, up to the next blank line.
   - `EXPORTER_FNS` is an array holding the seven names from step 6. Later plans append to it.
   Then build a fresh instance X = loadApp(APP_PATH) with X.DB = populatedDB(X). Override `X.__sandbox.document.createElement` to return plain objects that record themselves in a `clicks` array when clicked. Override `X.__sandbox.Blob` with a constructor that records its parts and type. Add these oks, all labelled with the prefix "export: ":
   - "export: Settings shows Export for Claude (.md) directly below Export backup (.json) (D-02)". In X.viewData(), the JSON button text comes before the Claude button text, which comes before "Import backup". No other `<button` sits between the JSON button's closing tag and the Claude button. X.__src contains exactly one `onclick="exportMarkdown()"`.
   - "export: tapping it downloads ppl-export-2026-08-07.md as text/markdown (D-03/EXP-01)". X.exportMarkdown() returns 'download', one anchor was clicked, its download is 'ppl-export-2026-08-07.md' and the Blob type is 'text/markdown'.
   - "export: the downloaded text is exactly buildMarkdownExport()"
   - "export: the file opens with its title and a Generated timestamp". The text starts with '# PPL Tracker export' followed by a newline and contains the line '- Generated: 2026-08-07T17:00:00.000Z'.
   - "export: Workouts header reads date | workout | exercise | set | weight (lb) | reps (D-08/D-10)"
   - "export: Cardio header reads date | type | minutes | distance (km) | note (D-08)"
   - "export: a live weigh-in reaches Weigh-ins through liveOf (EXP-04)". A row has cells [dayOff(-9), '196.4'].
   - "export: the soft-deleted LEGS 2 session is absent (EXP-04)". No Workouts row has workout 'LEGS 2'.
   - "export: every table row has its header's cell count, even with the hostile idea text (EXP-07)"
   - "export: the JSON backup still downloads ppl-backup-2026-08-07.json and records lastBackupAt (EXP-01)". Call X.exportData() on the same overrides and check the recorded anchor download and a numeric X.DB.lastBackupAt.
   A FAIL extra names only the section or the cell count, never a whole file.

9. Run the verify command, commit with a message describing "COLLECTIONS columns become {field,label,unit}; Markdown export tracer from Settings", and stop for the tracer feedback gate when the run is interactive.
  </action>
  <verify>
    <automated>TZ=America/Chicago node test/app.test.js > /tmp/p0201-1.txt; echo "exit=$?"; grep -c 'PASS  export: ' /tmp/p0201-1.txt; tail -1 /tmp/p0201-1.txt</automated>
    <human-check>Tracer feedback gate (interactive runs only): open index.html from the repo root you are executing in, in a desktop browser (directly, or via `python -m http.server` and localhost). Go to Settings → Backup and tap "Export for Claude (.md)". A file named ppl-export-YYYY-MM-DD.md downloads. It opens with "# PPL Tracker export" and a Generated line, then has one "## " section per collection, Workouts first and Sleep last, with "No entries" under any empty section. If no browser is handy, approving on the automated output alone is acceptable.</human-check>
  </verify>
  <acceptance_criteria>
    - The command prints exit=0, and the last line of /tmp/p0201-1.txt matches `^[0-9]+ passed, 0 failed, [0-9]+ skipped$`.
    - `grep -c 'PASS  export: ' /tmp/p0201-1.txt` prints at least 10.
    - /tmp/p0201-1.txt contains `PASS  export: Settings shows Export for Claude (.md) directly below Export backup (.json) (D-02)` and `PASS  export: tapping it downloads ppl-export-2026-08-07.md as text/markdown (D-03/EXP-01)`.
    - /tmp/p0201-1.txt contains `PASS  SLEEP-05: the declaration is valid` and `PASS  rows: every row's keys equal the COLLECTIONS.sessions column fields, in order`.
    - `grep -cE '^function (mdEscape|mdCell|mdHeader|exportRows|buildMarkdownExport|exportMarkdown|downloadMarkdown)\(' index.html` prints 7.
    - `grep -c 'onclick="exportMarkdown()"' index.html` prints 1.
    - `grep -c "columns:\[{field:" index.html` prints 11.
    - `grep -c "label:'Workouts'" index.html` prints 1.
    - `grep -c "'buildMarkdownExport'" test/harness.js` prints 1.
  </acceptance_criteria>
  <done>Every COLLECTIONS entry declares a label and column objects, and collectionProblems accepts the shipped registry under the new rules. Tapping "Export for Claude (.md)" in Settings downloads ppl-export-YYYY-MM-DD.md, built generically from COLLECTIONS and liveOf with labelled, unit-resolved headers and "No entries" for empty sections. The JSON backup is unchanged. Every Phase 1 check still passes and the change is committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Registry contract for the new column shape, and the EXP-02 proofs (probe collection, static exporter check, empty/adjacent/ordered sections)</name>
  <files>index.html, test/app.test.js</files>
  <read_first>
    - index.html: collectionProblems as rewritten in Task 1, and the exporter functions after exportData()
    - test/app.test.js: the refusal battery (validListSpec/validMapSpec block), the SLEEP-05 probe block, and the "export for Claude" section written in Task 1
    - .planning/phases/02-export-for-claude/02-RESEARCH.md sections "Don't Hand-Roll" and "Anti-Patterns to Avoid"
  </read_first>
  <behavior>
    - collectionProblems({ thing: validListSpec() with columns ['date'] }) is non-empty. A column that is a plain string is refused.
    - A column with no field is refused, and so is a column with no label.
    - Columns whose field is 'id', 'mtime' or 'deletedAt' are each refused, and the problem names that field.
    - A column {field:'date', label:'date', lable:'x'} is refused, and the problem names 'lable'.
    - A column with unit '' is refused, and so is a column with unit 5.
    - A collection label 'A|B' is refused, and so is a column label containing a newline.
    - A collection with no label is refused.
    - Two columns sharing the label 'date' are refused.
    - collectionProblems({ a: listSpec with label 'Same', b: mapSpec with label 'Same' }) yields one problem starting 'a:' and one starting 'b:', and each mentions 'Same'.
    - Booting the SLEEP-05 probe and setting probe.DB to blank plus probeList rows p1 (2026-08-01, value 1, live) and p2 (2026-08-02, value 2, deletedAt 5), and probeMap {'2026-08-03': {a:true}}, then calling probe.buildMarkdownExport(), gives a 'Probe list' section with header [date, value (lb)] and exactly one row ['2026-08-01','1'], and a 'Probe map' section with exactly one row whose first cell is '2026-08-03'.
    - The '## ' headings of that probe export, in order, equal Object.keys(probe.COLLECTIONS) mapped to their labels. Probe list and Probe map come first (D-12).
    - A fresh install (blank DB) exports exactly Object.keys(app.COLLECTIONS).length headings, every section is empty with 'No entries', and nothing throws (D-06).
    - The smoke-fixture export has separate 'Mobility' and 'Lawn' sections: Mobility has 1 row and Lawn has 2 (EXP-02 adjacency).
    - For every name in EXPORTER_FNS, the comment-stripped app[name].toString() contains no COLLECTIONS key as a whole word, no named wrapper (liveSessions, liveWeights, livePetWeights, liveCardio, liveIdeas, liveTodos, liveHobbyLog), no quoted 'lb', 'kg' or 'km' in either quote style, and no fmtDate, kmToDisp or esc( call.
    - For every name in EXPORTER_FNS, the comment-stripped source contains no save(, saveLocal(, touch(, lastBackupAt, backupSnoozeAt, fetch(, firebase, pushNow or runTransaction.
    - exportRows's comment-stripped source contains liveOf(.
  </behavior>
  <action>
Implements the registry-contract half of D-08, EXP-02's proofs (D-06, D-12, adjacency), and the declaration-side guard of EXP-05. It also enforces both of this plan's prohibitions by static test.

1. Write every behavior bullet as its own ok before touching index.html, and run the suite. Refusal bullets that Task 1's validator already enforces pass at once, which is expected. Any that fail are gaps in Task 1's rewrite: fix them in collectionProblems, never by weakening the test.
   - The "registry: " oks go inside the existing refusal-battery block, after the duplicate-columns refusal. Build them from validListSpec/validMapSpec and label them:
     - "registry: a column that is not an object is refused"
     - "registry: a column with no field is refused"
     - "registry: a column with no label is refused"
     - "registry: a column declaring field id is refused, naming it (EXP-05)", with the same form for mtime and deletedAt
     - "registry: an unknown column key is refused and named"
     - "registry: a column unit that is empty or not a string is refused"
     - "registry: a label holding | or a line break is refused"
     - "registry: a collection with no label is refused"
     - "registry: two columns repeating a label are refused"
     - "registry: two collections sharing a label are refused, naming both"
   - The "export: " oks go at the end of the "export for Claude" section:
     - "export: a probe collection declared in one line exports its own section with no exporter edit (EXP-02)"
     - "export: the probe's mass column is labelled from DB.unit and its deleted row is absent (EXP-02/EXP-04)"
     - "export: a probe map exports its own section (EXP-02)"
     - "export: sections follow COLLECTIONS order (D-12)"
     - "export: a fresh install exports every section as No entries (D-06)"
     - "export: Mobility and Lawn share dayFlagRows but export as two sections (EXP-02 adjacency)"
     - "export: no exporter function names a collection, a unit, a live wrapper, fmtDate, kmToDisp or esc (EXP-02)"
     - "export: no exporter function persists, touches backup bookkeeping or reaches the network"
     - "export: exportRows reads lists through liveOf (EXP-04)"
   The probe test reuses the top-level `probe` instance from the SLEEP-05 block. The static checks strip block and line comments first, the same way the SLEEP-04 check does. A FAIL extra lists only the offending function names and tokens.

2. Make any failing bullet pass by fixing collectionProblems or the exporter functions. The exporter must stay generic: no name, label, field or unit of any real collection may appear in its code.

3. Run the verify command and commit with a message describing "registry contract for column objects; EXP-02 probe and static proofs".
  </action>
  <verify>
    <automated>TZ=America/Chicago node test/app.test.js > /tmp/p0201-2.txt; echo "exit=$?"; grep -c 'PASS  export: ' /tmp/p0201-2.txt; grep -c 'PASS  registry: ' /tmp/p0201-2.txt; tail -1 /tmp/p0201-2.txt</automated>
  </verify>
  <acceptance_criteria>
    - The command prints exit=0, and the last line of /tmp/p0201-2.txt matches `^[0-9]+ passed, 0 failed, [0-9]+ skipped$`.
    - `grep -c 'PASS  export: ' /tmp/p0201-2.txt` prints at least 19.
    - /tmp/p0201-2.txt contains `PASS  export: a probe collection declared in one line exports its own section with no exporter edit (EXP-02)`, `PASS  export: no exporter function names a collection, a unit, a live wrapper, fmtDate, kmToDisp or esc (EXP-02)` and `PASS  export: no exporter function persists, touches backup bookkeeping or reaches the network`.
    - /tmp/p0201-2.txt contains `PASS  registry: a column declaring field id is refused, naming it (EXP-05)` and `PASS  registry: two collections sharing a label are refused, naming both`.
    - `grep -c 'EXPORTER_FNS' test/app.test.js` prints at least 3.
  </acceptance_criteria>
  <done>collectionProblems refuses every malformed column object, internal-id columns, and missing or duplicate labels. A probe list and probe map declared by source transform export their own sections with no exporter edit. Sections follow COLLECTIONS order, a fresh install exports every section as "No entries", and static checks prove the exporter names no collection or unit and never persists or reaches the network. The suite is green and the change is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored DB → Markdown text | Strings in DB may come from a hand-edited backup (validateBackup checks shape, not types) or from another device, and they land inside a structured table |
| developer edit → COLLECTIONS → export | A column declaration decides what leaves the app; a bad or leaky declaration reaches Ian's phone unless the suite catches it |
| app → download | The file leaves the app's storage for the device's file system |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-01 | Tampering | mdEscape / mdCell: a logged value holding `|`, a backslash before `|`, or a line break splits or forges table rows | medium | mitigate | mdEscape doubles the backslash run before every pipe and escapes it, and collapses line-break runs to `<br>`. Task 1's cell-count check over the smoke fixture; plan 02-03 adds the full EXP-07 battery |
| T-02-02 | Information disclosure | COLLECTIONS column declarations: a column for id, mtime or deletedAt would put internal ids in the file | medium | mitigate | collectionProblems refuses those fields (Task 2 tests). exportRows projects only declared fields, so undeclared row keys can never reach the file |
| T-02-03 | Tampering | Export path persisting state: save() bumps updatedAt, so an exporting device looks newest to sync (CLAUDE.md incident class) | high | mitigate | No exporter function calls save/saveLocal/touch or sets backup bookkeeping; Task 2's static check fails the suite if one does |
| T-02-04 | Denial of service | Module-eval registry validation: a malformed column object throws `COLLECTIONS is invalid` at boot and kills the app on the phone | high | mitigate | The suite boots the real app (loadApp) and the SLEEP-05 probe under the new rules, so a bad registry turns CI red and blocks the deploy job |
| T-02-05 | Tampering | Reusing esc() in the exporter HTML-encodes plain text (`&lt;`) in a file Claude reads as Markdown | low | mitigate | Task 2's static check forbids esc( in every exporter function |
| T-02-06 | Information disclosure | Export path transmitting data by itself (network, Firestore) | medium | mitigate | Task 2's static check forbids fetch, firebase, pushNow and runTransaction in every exporter function |

No package installs in this phase, so there is no T-02-SC row.
</threat_model>

<verification>
- `TZ=America/Chicago node test/app.test.js` exits 0 and ends `N passed, 0 failed, M skipped`.
- `git log --format=%s -2` shows this plan's two commits. `git diff HEAD~2 --stat` touches only index.html, test/harness.js and test/app.test.js.
- Every check that passed before this plan still passes. Only the Phase 1 checks listed in Task 1 step 7 changed, and each keeps its intent under the new column shape.
</verification>

<success_criteria>
- ROADMAP Phase 2 success criterion 2 holds: the table builder derives from COLLECTIONS, and adding a collection needs no exporter edit, proven by the probe test and the static check.
- Criterion 1 holds in part: Ian can export a Markdown file separate from the JSON backup. The header's date range lands in plan 02-03.
- D-02, D-03, D-06, D-08, D-09, D-10 and D-12 are implemented and tested.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Where |
|---|---|---|
| `COLLECTIONS[*].label` | registry field (required, distinct) | index.html, COLLECTIONS literal |
| `COLLECTIONS[*].columns` as `{field,label,unit?}[]` | registry field shape (promoted from `string[]`) | index.html, COLLECTIONS literal |
| `collectionProblems()` column-object, internal-field and label rules | validator branch | index.html, collectionProblems |
| `mdEscape(s)`, `mdCell(v)`, `mdHeader(col)` | function declarations | index.html, after dayFlagRows |
| `exportRows(name)`, `buildMarkdownExport()`, `downloadMarkdown(text, filename)`, `exportMarkdown()` | function declarations | index.html, after exportData |
| "Export for Claude (.md)" button | Settings markup | index.html, viewData Backup card |
| harness `names` additions (seven exporter names) | test harness config | test/harness.js |
| `mdCells(line)`, `mdSections(md)`, `EXPORTER_FNS` | test helpers/constant | test/app.test.js, "export for Claude" section |

## Edge coverage tally (spec-less probe fallback)

This plan authors 3 of the phase's 20 edge-probe items as explicit plain truths: EXP-02 adjacency, EXP-02 empty and EXP-02 ordering.

Phase total: 20 = 18 explicit + 0 backstop + 2 flagged assumptions (EXP-05 unclassified in plan 02-02, EXP-06 unclassified in plan 02-03).

Prohibitions: this plan carries 2 of the phase's 5 recalled prohibitions (EXP-01 safety, EXP-02 privacy). They are descriptor-less and each is backed by Task 2's static test. Canon referral: Markdown table injection is the canon injection class, so it is not minted as a prohibition; it is covered by the EXP-07 truths and T-02-01.

## Flagged assumptions (for Ian at verify time)

- **Line breaks inside a cell become `<br>`.** EXP-07 says newlines are "replaced" but not with what. `<br>` keeps a multi-line journal day readable in Claude and in any Markdown viewer, and it cannot break the table. The alternative is a plain space, which is flatter but loses the line structure. The change is reversible and export-only.

<output>
Create `.planning/phases/02-export-for-claude/02-01-SUMMARY.md` when done.
</output>
