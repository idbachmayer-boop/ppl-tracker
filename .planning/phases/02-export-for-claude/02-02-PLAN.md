---
phase: 02-export-for-claude
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - index.html
  - test/app.test.js
autonomous: true
requirements: [EXP-03, EXP-04, EXP-05, EXP-06]

estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "D-04: a skipped workout day exports as exactly one Workouts row, date | workout | (skipped) | — | — | —. sessionRows returns that single row, with keys equal to the sessions column fields, for any session whose skipped is truthy, even one that carries entries; the skip reason is not exported"
    - "D-05: Mobility and Lawn export only flags whose value is exactly true, as date + item with no done column. Keys starting with __ (mobilityLog's __session) and camelCase override keys (lawnLog's overrideWater / overrideMow) are dropped by a separate check that runs before the true-only check"
    - "D-11: rows in every table run oldest first by the first column, compared as plain strings. The sort is stable, so rows with equal dates keep the order liveOf (or the sorted map keys) produced. A missing first-column value sorts first and is written as —"
    - "D-09: a column declaring zeroIsMissing: true writes — for a stored 0. Cardio's minutes and distance columns declare it, because addCardio stores 0 for whichever of the two Ian left blank. Every other column writes a real 0 as 0"
    - "D-07: nothing outside COLLECTIONS reaches the file: not the pet name, the lawn location (DB.lawn lat, lon and label), the hobby or productivity lists, the exercise registry, the in-progress draft or the weather cache"
    - "EXP-03 boundary: set numbers start at 1 and count up within one exercise; a live, non-skipped session with no sets contributes zero rows; a skipped session contributes exactly one"
    - "EXP-03 adjacency: two identical sets in one exercise export as two rows (set 1 and set 2), never merged, and two sessions on the same date export as separate row groups in stored order"
    - "EXP-03 empty: a set stored with w '' and r '' exports — for weight and for reps"
    - "EXP-03 ordering: within one session, rows follow the entries, then the extras, with set numbers ascending, and that order survives the oldest-first sort"
    - "EXP-03 precision: weight and reps are written exactly as stored (187.5, '0', '135'), with no rounding, no lb/kg conversion and no numeric coercion"
    - "EXP-04 adjacency: when a soft-deleted row shares its date with a live row, exactly the live row appears, once"
    - "EXP-04 empty: a list collection missing from DB, a map collection holding a non-object, and a null map each export 'No entries' without throwing"
    - "EXP-04 ordering: liveOf removes soft-deleted rows before the sort, and deleting a row with softDelete and exporting again drops it from the file (ROADMAP Phase 2 criterion 3)"
    - "EXP-05: no table has an id, mtime, deletedAt or source column, and no internal id value (s1, c9, i1, sl1) and no deletedAt text appears anywhere in the file"
    - "collectionProblems allows the optional column key zeroIsMissing and refuses it when it is not a boolean"
  artifacts:
    - path: "index.html"
      provides: "sessionRows skipped-day row, dayFlagRows true-only + bookkeeping filter, date+item day-flag columns, zeroIsMissing on cardio columns, oldest-first stable sort in exportRows, mdCell(v, col)"
      contains: "(skipped)"
    - path: "test/app.test.js"
      provides: "D-04/D-05 row tests, EXP-03/EXP-04/EXP-05/D-07/D-09/D-11 export tests"
      contains: "D-11"
  key_links:
    - from: "COLLECTIONS.sessions.format"
      to: "sessionRows skipped branch"
      via: "hoisted function reference; exportRows dispatches to it"
      pattern: "'\\(skipped\\)'"
    - from: "exportRows(name)"
      to: "stable first-column sort"
      via: "sort of the projected rows by row[columns[0].field]"
      pattern: "columns\\[0\\]"
    - from: "buildMarkdownExport()"
      to: "mdCell(value, column)"
      via: "column spec passed so zeroIsMissing applies"
      pattern: "mdCell\\([^)]*,"
  prohibitions:
    - statement: "MUST NOT put a soft-deleted row, an internal id or sync bookkeeping into the file (id, mtime, deletedAt, source, __session, override flags, explicit-false flags). Ian deleted or never logged it, so Claude must not see it"
      requirement_id: EXP-05
      category: privacy
      status: resolved
      verification: test
    - statement: "MUST NOT export anything outside COLLECTIONS, and in particular never the lawn location (DB.lawn latitude, longitude and label, which is where Ian lives), the pet name, the hobby and productivity lists, the exercise registry, the in-progress draft or the weather cache"
      requirement_id: EXP-02
      category: privacy
      status: resolved
      verification: test
---

<objective>
Make each table's content correct. Task 1: a skipped workout day becomes one row (D-04). Mobility and Lawn export only what Ian actually ticked, with no bookkeeping keys and no done column (D-05). The workout flattening edges are pinned (EXP-03). Task 2: rows run oldest first with a stable sort (D-11). A stored "not entered" 0 in cardio becomes the missing marker (D-09). Deleted rows, internal ids and anything outside COLLECTIONS are proven absent (EXP-04, EXP-05, D-07).

Purpose: plan 02-01 proved the pipe from registry to file. This plan makes sure what flows through it is what Ian logged: nothing he deleted, nothing that is bookkeeping, and nothing silently missing.

Output: changes to sessionRows, dayFlagRows, the mobilityLog/lawnLog/cardio column declarations, exportRows (sort), mdCell (column rules) and collectionProblems (zeroIsMissing), plus their tests.

This plan has no checkpoint and no tracer task, so it runs without stopping. Keep the suite green at every commit. Do not push to `main`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/02-export-for-claude/02-CONTEXT.md
@.planning/phases/02-export-for-claude/02-RESEARCH.md
@.planning/phases/02-export-for-claude/02-01-SUMMARY.md
@CLAUDE.md
@index.html
@test/app.test.js

<interfaces>
From plan 02-01 (read 02-01-SUMMARY.md for any drift):
- Column spec `{field, label, unit?}`. Every collection has `label`. mobilityLog columns are date→date, item→stretch, done→done. lawnLog columns are date→date, item→task, done→done. cardio columns are date, type, minutes, distanceKm (label distance, unit 'km') and note.
- `exportRows(name)` returns projected rows. Lists come from liveOf(name) and maps from Object.keys(DB[name]).sort(). There is no row sort yet.
- `mdCell(v)` returns '—' for null, undefined or blank values. It JSON-encodes objects, then applies mdEscape.
- `buildMarkdownExport()` builds a title, a description line, a Generated line, then one '## label' section per collection with a table or 'No entries'.
- test/app.test.js has the "── export for Claude (EXP-01…EXP-08) ──" section with `mdCells(line)`, `mdSections(md)` and `EXPORTER_FNS`, placed just before the REG-01 "never mutated" block.

Existing:
- `sessionRows(s)` index.html ~3797. `dayFlagRows(date, obj)` ~3815. `skipDay()` ~1717 pushes `{..., skipped:true, reason, entries:[], stairs:null}`.
- `toggleMobSession()` sets `mobilityLog[date].__session` to 'yoga' or false. `toggleLawnOverride()` sets `lawnLog[date].overrideWater` or `overrideMow` to true or false. `isReservedMobKey = k => k.indexOf('__')===0`.
- `addCardio()` ~3561 stores `minutes:min` (0 when left blank) and `distanceKm:(distV>0 ? dispToKm(distV) : 0)`.
- `addIdea()` unshifts, so stored ideas run newest first. `softDelete(arr, pred)` is exported by the harness.
- test/app.test.js: in the REG-17 block, the Phase 1 ok "rows: dayFlagRows returns one row per key, including done false" (~1907-1909); `PROBE_MAP_LINE` (~1501); `populatedDB(a)`, where s3 is a skipped PULL 1 on dayOff(-5) with reason 'slept in', s5 is a soft-deleted LEGS 2, mobilityLog is {today: {'Couch stretch': true}}, and lawnLog is {dayOff(-4): {mow:true}, dayOff(-1): {water:true}}; `draftFor(a, workout)`.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Skipped days as one row (D-04), true-only day flags without bookkeeping keys (D-05), and the workout-flattening edges (EXP-03)</name>
  <files>index.html, test/app.test.js</files>
  <read_first>
    - index.html: sessionRows, hobbyRows, journalRows and dayFlagRows (the REG-17 row-shaper block, ~3793-3818) and the COLLECTIONS literal (~489-505)
    - index.html: skipDay (~1717), toggleMobSession and isReservedMobKey (~2682-2724), toggleLawnOverride (~3155)
    - test/app.test.js: the REG-17 rows block (~1884-1912), PROBE_MAP_LINE (~1501), populatedDB (~2053) and the "export for Claude" section
    - .planning/phases/02-export-for-claude/02-RESEARCH.md: Pitfalls 2, 3 and 4
  </read_first>
  <behavior>
    - sessionRows({id:'k', date:'2026-08-02', workout:'PULL 1', skipped:true, reason:'slept in', entries:[]}) deep-equals [{date:'2026-08-02', workout:'PULL 1', exercise:'(skipped)', set:null, weight:null, reps:null}].
    - The same session with skipped true and one entry holding a set still returns exactly that one skipped row.
    - The skipped row's Object.keys equal the sessions column fields in order.
    - The smoke-fixture export's Workouts section has a row with cells [dayOff(-5), 'PULL 1', '(skipped)', '—', '—', '—'], and no cell in the file equals 'slept in'.
    - dayFlagRows('2026-08-01', {a:true, b:false}) deep-equals [{date:'2026-08-01', item:'a'}].
    - dayFlagRows('d', {'Couch stretch':true, __session:'yoga'}) deep-equals [{date:'d', item:'Couch stretch'}].
    - dayFlagRows('d', {mowed:true, overrideMow:true, overrideWater:false, watered:false}) deep-equals [{date:'d', item:'mowed'}].
    - dayFlagRows('d', {a:'yes', b:1}) returns []. dayFlagRows('d', {}) returns []. dayFlagRows('d', 'x') still returns [].
    - The smoke-fixture export's Mobility header is [date, stretch], its Lawn header is [date, task], and Lawn still has 2 rows.
    - A session with exercise A holding two identical sets {w:'135', r:'8'} exports two rows, with set cells '1' and '2'.
    - A live, non-skipped session with entries [] and extras {} exports no rows, so Workouts reads 'No entries' when it is the only session.
    - Sets {w:187.5, r:5}, {w:'0', r:'12'} and {w:'', r:''} export weight/reps cells '187.5'/'5', '0'/'12' and '—'/'—'.
    - A session with entries [A (2 sets), B (1 set)] and extras {forearms: C (1 set)} exports rows in the order A1, A2, B1, C1.
  </behavior>
  <action>
Implements D-04, D-05 and EXP-03's edges.

1. Write the tests first and run the suite; expect the D-04 and D-05 oks to fail.
   - "rows: " oks go in the REG-17 block. Replace the Phase 1 ok "rows: dayFlagRows returns one row per key, including done false" with "rows: dayFlagRows keeps only true flags, as date + item (D-05)", which asserts the new deep-equal. Add:
     - "rows: a skipped session is one row — (skipped), no set, weight or reps (D-04)"
     - "rows: a skipped session with entries is still one row (D-04)"
     - "rows: the skipped row's keys equal the sessions column fields"
     - "rows: dayFlagRows drops __ keys such as __session (D-05)"
     - "rows: dayFlagRows drops override keys and false flags (D-05)"
     - "rows: dayFlagRows ignores truthy values that are not true (D-05)"
     - "rows: dayFlagRows on an empty day returns []"
   - "export: " oks go at the end of the "export for Claude" section:
     - "export: a skipped workout day is one Workouts row (D-04)"
     - "export: Mobility and Lawn export date + item only (D-05)"
     - "export: sets number from 1 within an exercise (EXP-03 boundary)"
     - "export: a live session with no sets contributes no rows (EXP-03 boundary)"
     - "export: two identical sets stay two rows (EXP-03 adjacency)"
     - "export: weight and reps are written exactly as stored (EXP-03 precision)"
     - "export: an unfilled set writes — for weight and reps (EXP-03 empty/D-09)"
     - "export: rows keep entries-then-extras order, sets ascending (EXP-03 ordering)"
   The export-level oks build their own instance from loadApp(APP_PATH) with DB = Object.assign(blank(), { sessions:[...] }) and read cells through mdSections.

2. sessionRows (D-04). Directly after the existing object guard, when s.skipped is truthy, return exactly one row with keys in the sessions column order: date s.date, workout s.workout, exercise the literal '(skipped)', and set, weight and reps each null. Null becomes the em dash at presentation. Return it whether or not entries or extras are present, because a day Ian skipped records no sets. Add one sentence to the REG-17 comment: a skipped session is one row, so a skipped day is never silently absent.

3. dayFlagRows (D-05). Return one {date, item: key} row per own key of obj, in key order, for each key that passes two independent checks run in this order (RESEARCH Pitfall 3: never fold them into one condition).
   - (a) Bookkeeping keys are dropped: any key starting with '__', which is mobilityLog's __session and the same convention as isReservedMobKey, and any key matching /^override[A-Z]/, which covers lawnLog's overrideWater and overrideMow written by toggleLawnOverride.
   - (b) Only a value strictly equal to true is a logged item. An explicit false is sync bookkeeping ("absence never means off", CLAUDE.md), not an event.
   Keep the existing non-object guard. Replace the function's comment with one that names both checks and why each exists.

4. COLLECTIONS: mobilityLog columns become date→date and item→stretch. lawnLog columns become date→date and item→task. The done column goes away (D-05). Keep each entry on one line.

5. test/app.test.js: remove the done column from `PROBE_MAP_LINE` (leaving date→date and item→item), so the probe's declared columns match what dayFlagRows now returns.

6. Run the suite until it is green, then commit with a message describing "skipped days as one export row; day flags export only what was ticked".
  </action>
  <verify>
    <automated>TZ=America/Chicago node test/app.test.js > /tmp/p0202-1.txt; echo "exit=$?"; grep -cE 'PASS  (rows|export): .*(D-04|D-05|EXP-03)' /tmp/p0202-1.txt; tail -1 /tmp/p0202-1.txt</automated>
  </verify>
  <acceptance_criteria>
    - The command prints exit=0, and the last line of /tmp/p0202-1.txt matches `^[0-9]+ passed, 0 failed, [0-9]+ skipped$`.
    - `grep -cE 'PASS  (rows|export): .*(D-04|D-05|EXP-03)' /tmp/p0202-1.txt` prints at least 13.
    - /tmp/p0202-1.txt contains `PASS  export: a skipped workout day is one Workouts row (D-04)` and `PASS  rows: dayFlagRows drops override keys and false flags (D-05)`.
    - /tmp/p0202-1.txt contains `PASS  SLEEP-05: the declaration is valid` and `PASS  SLEEP-05: the map probe replaces whole days and keeps explicit false`.
    - `grep -c "'(skipped)'" index.html` prints at least 1.
    - `grep -c "label:'stretch'" index.html` prints 1 and `grep -c "label:'task'" index.html` prints 1.
  </acceptance_criteria>
  <done>A skipped workout day exports as one "(skipped)" row. Mobility and Lawn export only ticked items as date + item, with __session, override keys and explicit false dropped. The workout-flattening edges (numbering, identical sets, empty sessions, stored precision, blank sets, entries-then-extras order) are pinned by tests. The suite is green and committed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Oldest-first stable order (D-11), zero-as-missing for cardio (D-09), and proof that deleted rows, internal ids and non-registry data never reach the file (EXP-04, EXP-05, D-07)</name>
  <files>index.html, test/app.test.js</files>
  <read_first>
    - index.html: exportRows, mdCell and buildMarkdownExport (from plan 02-01), collectionProblems' columns branch, the cardio COLLECTIONS entry, and addCardio (~3561)
    - index.html: liveOf and softDelete (~712-736), addIdea (~4373)
    - test/app.test.js: the "export for Claude" section, populatedDB (~2053) and draftFor (~2119)
    - .planning/phases/02-export-for-claude/02-CONTEXT.md: D-07, D-09 and D-11
  </read_first>
  <behavior>
    - With ideas stored newest first as [i3 2026-08-05 'third', i2 2026-08-03 'second', i1 2026-08-01 'first'], the Ideas rows' first cells are ['2026-08-01', '2026-08-03', '2026-08-05'].
    - Two sessions on 2026-08-01 stored as PUSH 1 (endedAt 1) then PULL 1 (endedAt 2) export every PUSH 1 row before every PULL 1 row.
    - Two ideas on the same date stored as 'x' then 'y' export 'x' first.
    - A journal stored as {'2026-08-05':'b', '2026-08-01':'a'} exports 2026-08-01 first.
    - An idea with no date exports first, with a first cell of '—'.
    - Weights [{date:'2026-08-01', value:190, deletedAt:5, mtime:5}, {date:'2026-08-01', value:191}] export exactly one Weigh-ins row, ['2026-08-01', '191'].
    - With the smoke fixture, the first export's Sleep section has 1 row. After softDelete(DB.sleep, s => s.id === 'sl1'), a second export's Sleep section is empty ('No entries').
    - A DB with no cardio key, journal set to 'x' and lawnLog set to null exports without throwing, and Cardio, Journal and Lawn each read 'No entries'.
    - The smoke fixture plus cardio {id:'c9', date:dayOff(-2), type:'Walk', minutes:20, distanceKm:1.5, note:'n', source:'manual', mtime:1} exports no header cell equal to id, mtime, deletedAt or source, no cell equal to 's1', 'c9', 'i1' or 'sl1', and no occurrence of the text deletedAt or mtime anywhere.
    - The smoke fixture with petName 'Rover-Unique-Pet', lawn {lat:44.9412, lon:-93.3611, label:'Home-Label-Unique'}, exercises [{id:'x1', name:'Unique Registry Lift'}] and a draftFor('PUSH 1') draft whose sessionNote is 'Draft-Note-Unique' exports none of 'Rover-Unique-Pet', '44.9412', '-93.3611', 'Home-Label-Unique', 'Unique Registry Lift', 'Draft-Note-Unique' or '🧊 Clean out the fridge' (an unlogged productivity default).
    - Cardio [{id:'c2', date:'2026-08-02', type:'Longboard', minutes:30, distanceKm:0, note:''}, {id:'c3', date:'2026-08-03', type:'Walk', minutes:0, distanceKm:3.2, note:'x'}] exports rows ['2026-08-02','Longboard','30','—','—'] and ['2026-08-03','Walk','—','3.2','x'].
    - A session set {w:0, r:10} still exports a weight cell '0'.
    - collectionProblems refuses a column with zeroIsMissing: 'yes' and accepts one with zeroIsMissing: true.
  </behavior>
  <action>
Implements D-11, D-09's zero rule, D-07 and the EXP-04 and EXP-05 guarantees. It also backs this plan's two privacy prohibitions with tests.

1. Write the tests first and run the suite. Expect the D-11, D-09 and zeroIsMissing oks to fail; the EXP-04, EXP-05 and D-07 oks may already pass, since plan 02-01's projection and liveOf read give those guarantees, and pinning them is the point.
   - "export: " oks go at the end of the "export for Claude" section:
     - "export: rows run oldest first (D-11)"
     - "export: equal dates keep stored order — sessions (D-11 stable)"
     - "export: equal dates keep stored order — ideas (D-11 stable)"
     - "export: map days run oldest first (D-11)"
     - "export: a row with no date sorts first and writes — (D-11/D-09)"
     - "export: a deleted row sharing a date with a live row leaves only the live row (EXP-04 adjacency)"
     - "export: deleting a row and exporting again drops it (EXP-04)"
     - "export: a missing list and a non-object or null map export No entries (EXP-04 empty)"
     - "export: no id, mtime, deletedAt or source column or value leaks (EXP-05)"
     - "export: nothing outside COLLECTIONS is exported (D-07)"
     - "export: cardio's blank minutes or distance writes — (D-09)"
     - "export: a column without zeroIsMissing still writes a real 0"
   - "registry: " oks go in the refusal battery:
     - "registry: a zeroIsMissing that is not a boolean is refused"
     - "registry: zeroIsMissing true is accepted"
   Each fixture uses its own loadApp instance. A FAIL extra names the section and the offending cell or token only.

2. exportRows (D-11). After projecting, stable-sort the rows by their first column's value, row[spec.columns[0].field]. Compare two values as strings, treating null or undefined as the empty string, using plain less-than and greater-than code-unit comparison. Never use localeCompare, which varies with the phone's locale. Array.prototype.sort is stable, so rows with equal dates keep the order liveOf or the sorted map keys produced: stored session order (sessionSort by endedAt) and, within one session, set order. Add a comment stating the contract: oldest first, stable, and a missing date first.

3. buildMarkdownExport: change the description line to 'Logged data from PPL Tracker, one Markdown table per collection, oldest first. Deleted entries and internal ids are not included, and — marks a missing value.'

4. Zero as missing (D-09).
   - mdCell becomes mdCell(v, col). When col is given, col.zeroIsMissing === true, and v is either a number equal to 0 or a string that is non-blank after trim and whose Number() is 0, return '—'. Otherwise keep the existing behaviour.
   - buildMarkdownExport passes the column spec as mdCell's second argument.
   - collectionProblems adds zeroIsMissing to the allowed column keys and refuses a non-boolean value with "columns[i].zeroIsMissing must be a boolean".
   - COLLECTIONS cardio: the minutes and distanceKm columns gain zeroIsMissing:true. Add a short comment explaining why: addCardio requires minutes or distance, stores 0 for the one left blank, and the Cardio view already shows — for zero distance, so exporting 0 would be a silent zero (EXP-06).

5. Run the suite until it is green, including plan 02-01's static EXPORTER_FNS checks, which the new code must still satisfy. Commit with a message describing "export rows oldest first; cardio blanks as —; deleted rows, ids and non-registry data proven absent".
  </action>
  <verify>
    <automated>TZ=America/Chicago node test/app.test.js > /tmp/p0202-2.txt; echo "exit=$?"; grep -cE 'PASS  export: .*(D-07|D-09|D-11|EXP-04|EXP-05)' /tmp/p0202-2.txt; tail -1 /tmp/p0202-2.txt</automated>
  </verify>
  <acceptance_criteria>
    - The command prints exit=0, and the last line of /tmp/p0202-2.txt matches `^[0-9]+ passed, 0 failed, [0-9]+ skipped$`.
    - `grep -cE 'PASS  export: .*(D-07|D-09|D-11|EXP-04|EXP-05)' /tmp/p0202-2.txt` prints at least 11.
    - /tmp/p0202-2.txt contains `PASS  export: nothing outside COLLECTIONS is exported (D-07)`, `PASS  export: no id, mtime, deletedAt or source column or value leaks (EXP-05)` and `PASS  export: rows run oldest first (D-11)`.
    - /tmp/p0202-2.txt contains `PASS  export: no exporter function names a collection, a unit, a live wrapper, fmtDate, kmToDisp or esc (EXP-02)`.
    - `grep -c "zeroIsMissing:true" index.html` prints at least 2.
    - `grep -cE '^function mdCell\(v, ?col\)' index.html` prints 1.
  </acceptance_criteria>
  <done>Every table runs oldest first with a stable, locale-independent sort. Cardio's "not entered" zeros export as —, and every other 0 stays 0. Soft-deleted rows, internal ids, sync bookkeeping and everything outside COLLECTIONS (home location, pet name, lists, exercise registry, draft) are proven absent from the file. The suite is green and committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| stored DB → Markdown text | Rows may carry sync bookkeeping (deletedAt, mtime, __session, override flags) and fields that were never meant for display |
| DB beyond COLLECTIONS → export | DB also holds non-registry state: the lawn location, the pet name, lists, the exercise registry, the draft, the weather cache and sync fields |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-02-07 | Information disclosure | dayFlagRows: bookkeeping keys (__session, overrideWater/overrideMow) and explicit false flags exported as if Ian had logged them | medium | mitigate | Two independent filters, bookkeeping-key first and then strict true; Task 1 row and export tests |
| T-02-08 | Information disclosure | exportRows / buildMarkdownExport: soft-deleted rows or internal ids (id, mtime, deletedAt, source) in the file | medium | mitigate | liveOf for lists, projection to declared columns only, and collectionProblems refusing internal fields (plan 02-01); Task 2's EXP-04/EXP-05 tests, including delete-then-re-export |
| T-02-09 | Information disclosure | Non-registry DB data, above all DB.lawn lat/lon (home location), reaching the export | high | mitigate | buildMarkdownExport iterates only Object.keys(COLLECTIONS); Task 2's D-07 test seeds unique markers into lawn, petName, exercises and draft and asserts none appear |
| T-02-10 | Tampering (data integrity) | A misleading export: a skipped day silently missing, or "not entered" written as 0 | low | mitigate | D-04 one-row skipped day and D-09 zeroIsMissing, both tested; flagged assumptions below cover the set-level nuances left as they are |
| T-02-11 | Denial of service | exportRows on a malformed DB (missing list, non-object map, entries not an array) throws, so the button does nothing | low | mitigate | liveOf's empty-array fallback, map non-object guard, shaper guards; Task 2's EXP-04 empty test |
</threat_model>

<verification>
- `TZ=America/Chicago node test/app.test.js` exits 0 and ends `N passed, 0 failed, M skipped`.
- `git log --format=%s -2` shows this plan's two commits. `git diff HEAD~2 --stat` touches only index.html and test/app.test.js.
- Plan 02-01's export, registry and static checks all still pass.
</verification>

<success_criteria>
- ROADMAP Phase 2 criterion 3 holds: after deleting a row, the export shows it absent along with its internal id, and deletedAt never appears.
- Criterion 4 holds: workouts export as one flat row per set, and a skipped day is one explicit row.
- D-04, D-05, D-07, D-09 (zero rule) and D-11 are implemented and tested.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Where |
|---|---|---|
| `sessionRows(s)` skipped-day branch (one `(skipped)` row) | function change | index.html, REG-17 row shapers |
| `dayFlagRows(date, obj)` bookkeeping-key filter + strict-true filter, returning `{date, item}` | function change | index.html, REG-17 row shapers |
| mobilityLog / lawnLog columns `date, item` (labels `stretch`, `task`) | registry change | index.html, COLLECTIONS |
| column key `zeroIsMissing` (optional boolean), set on cardio `minutes` and `distanceKm` | registry field + validator rule | index.html, COLLECTIONS and collectionProblems |
| `mdCell(v, col)` | function signature change | index.html, Markdown export presentation block |
| oldest-first stable sort in `exportRows(name)` | function change | index.html, after exportData |
| D-04/D-05/D-07/D-09/D-11/EXP-03/EXP-04/EXP-05 tests | tests | test/app.test.js, REG-17 block, refusal battery and "export for Claude" section |

## Edge coverage tally (spec-less probe fallback)

This plan authors 8 of the phase's 20 edge-probe items as explicit plain truths: EXP-03 boundary, adjacency, empty, ordering and precision, and EXP-04 adjacency, empty and ordering. It also surfaces 1 flagged assumption: EXP-05's unclassified probe (below).

Phase total: 20 = 18 explicit + 0 backstop + 2 flagged assumptions (EXP-05 here, EXP-06 in plan 02-03).

Prohibitions: this plan carries 2 of the phase's 5 recalled prohibitions (EXP-05 privacy and EXP-02/D-07 privacy). They are descriptor-less, and each is backed by Task 2's tests.

## Flagged assumptions (for Ian at verify time)

- **EXP-05 (probe category: unclassified), left unresolved on purpose.** The plan settles the mechanical part: no id, mtime, deletedAt or source, and no deleted rows. One judgement follows from D-05 and is Ian's to confirm. A logged yoga session is stored as `mobilityLog[date].__session = 'yoga'`, which is a reserved key and not `true`, so D-05's rule drops it from the export. If Ian wants yoga sessions visible to Claude, that needs a follow-up decision, for example a registry-declared way to export `__session` as a "yoga session" item.
- **Set-level detail is exported as stored.** sessionRows still emits every stored set (the Phase 1 REG-17 contract), so three things reach Claude without annotation. A set Ian marked skipped shows its stored weight and reps. A deload set looks like any other. Time-based holds such as dead hangs carry seconds in the `reps` column. The app's own views treat `skipped` sets and empty-reps sets as not real work. Say so if the export should drop or annotate them; it would be a new column or a filter in sessionRows.
- **Cardio distance is exported unrounded, in stored km.** A distance typed in miles is stored converted (for example `4.506163…`) and exported verbatim, per D-08's literal `distance (km)` and the no-conversion rule. Rounding would be a presentation choice for Ian to make.
- **Zero as missing applies only to cardio minutes and distance.** Those are the two fields where the app itself stores 0 to mean "not entered". Everywhere else, 0 is a real value (for example a 0 lb set).

<output>
Create `.planning/phases/02-export-for-claude/02-02-SUMMARY.md` when done.
</output>
