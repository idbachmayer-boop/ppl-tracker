---
phase: 01-f1-the-collections-registry
plan: 04
type: execute
wave: 4
depends_on: [01-03]
files_modified:
  - test/app.test.js
  - .gitignore
autonomous: true
requirements: [REG-09, REG-13]

estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "A seeded random battery of 200 two-device pairs, each merged with localWins false and with localWins true, gives identical legacy and derived output on every legacy collection and scalar"
    - "For the derived mergeDB, compared by content per key at the mergeDB level: merging a device with itself changes nothing, argument order does not change the result, and for every list collection merge(merge(A,B),C) equals merge(A,merge(B,C)), the chain a Firestore transaction retry performs"
    - "Map collections are documented as not associative today, because a map day carries no per-day mtime; legacy and derived agree on the counterexample, and the finding is recorded in the SUMMARY instead of being patched"
    - "When test/local/real-db-snapshot.json is present, the suite runs the legacy-vs-derived differential for validateBackup, all seven liveX filters, the real boot path and mergeDB over Ian's real data, printing only counts, booleans and collection or scenario names"
    - "When that file is absent, the suite prints a visible SKIP line naming the path, reports the skip in a separate count on the final summary line, and still exits 0"
    - "A .gitignore keeps test/local/ and every ppl-backup-*.json out of git, so the real backup can never be committed or published by the Pages upload of the whole checkout"
    - "REG-13 adjacency: the random generator draws keys from small pools, so same-key collisions, exact mtime ties, missing mtimes, soft-deleted rows, explicit-false map flags and gen differing by one all occur within the 200-pair battery"
    - "REG-13 empty: an absent real backup gives a visible '  SKIP  real-backup differential' line, a non-zero skipped count on the summary line and exit 0, while a present but unparseable file gives a FAIL, never a skip"
    - "REG-13 ordering: the battery is seeded (mulberry32, seed 20260911), so every machine and CI run the same 200 pairs in the same order"
  artifacts:
    - path: ".gitignore"
      provides: "the local-only rule for Ian's real backup"
      contains: "test/local/"
    - path: "test/app.test.js"
      provides: "mulberry32, genDevice, the random differential, the merge-law checks, the skip counter and the real-backup block"
      contains: "real-backup differential"
  key_links:
    - from: "test/app.test.js real-backup block"
      to: "test/local/real-db-snapshot.json"
      via: "fs.existsSync guard with a loud skip"
      pattern: "real-db-snapshot\\.json"
    - from: ".gitignore"
      to: "test/local/"
      via: "ignore rule, so git check-ignore exits 0"
      pattern: "^test/local/$"
    - from: "final summary line"
      to: "skip counter"
      via: "'N passed, N failed, N skipped' with the exit code driven by fail only"
      pattern: "skipped"
  prohibitions:
    - statement: "MUST NOT commit, deploy or print the contents of Ian's real backup (journal, weights, notes); the file lives only in the git-ignored test/local/, and the real-backup block reports counts, booleans and names only"
      category: privacy
      status: resolved
      verification: test
    - statement: "MUST NOT count a skipped real-backup run as passed; a skip is a visible SKIP line and a separate count, never part of the pass total"
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT change mergeUnion or mergeDateMap to make a merge-law check pass; a law that fails identically on mergeDB_legacy is a pre-existing finding to report, not a thing to patch in this phase (PITFALLS Pitfall 5)"
      category: safety
      status: resolved
      verification: judgment
    - statement: "MUST NOT push to main from this plan"
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Widen REG-13's differential beyond the hand-picked incident fixtures in two directions. Task 1 adds a seeded random battery of two-device merges, legacy against derived, plus merge-law checks written at the mergeDB level as PITFALLS Pitfall 5 prescribes. Task 2 adds the real-backup leg over Ian's actual exported data.

Per Ian's decision 3 (2026-09-11), the real backup is local only. The repo and the live site are public, and CI uploads the whole checkout to Pages, so a committed backup would publish Ian's journal, weights and notes. This plan therefore:
- creates the repo's first `.gitignore`, covering the local-only path
- makes the real-data block run only when the file is present
- makes it skip loudly when the file is absent: a visible SKIP line and a separate skipped count, never a silent pass, and never a failure that blocks CI

Getting the file from Ian is plan 01-06's `checkpoint:human-action`. This plan never fabricates one.

Output: the random battery, the merge-law checks, `.gitignore`, the skip counter and the real-backup block.

Constraint: do not push to main.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-f1-the-collections-registry/01-RESEARCH.md
@.planning/phases/01-f1-the-collections-registry/01-03-SUMMARY.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@.github/workflows/deploy.yml
@test/harness.js
@test/app.test.js

<interfaces>
From plans 01-01 to 01-03, in test/app.test.js:
- `ok(name, cond, extra)`, whose `extra` is printed as JSON on FAIL
- `app`, `canon(v)`, `LEGACY_COLLECTIONS`, `clone(x)`, `legacyView(out)`, `sameMerge(...)`, `ROW_FOR`
- the final two lines: the summary console.log and `process.exit(fail ? 1 : 0)`

From index.html, exported through the harness: `mergeDB`, `mergeDB_legacy`, `validateBackup`, `validateBackup_legacy`, the seven liveX wrappers and their seven `_legacy` twins, `normalize`, `blank`, `COLLECTIONS`, `sessKey`, `cardioKey`, `ideaKey`, `todoKey`, `hobbyKey`.

loadApp(APP_PATH, seed) accepts a seed that is a JSON string or an object.

Created by this plan:
- `mulberry32(seed)` returns a function giving floats in [0, 1).
- `genDevice(rand, mode)` returns a DB object. mode is 'differential' or 'laws'.
- `contentOf(db)` returns a canon string of per-key content: list rows by their key including tombstones, map days by value, and journal days as a sorted set of trimmed lines.
- `skip` is a counter and `skipLine(msg)` prints the SKIP line and increments it.
- `REAL_PATH = path.join(__dirname, 'local', 'real-db-snapshot.json')`.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Seeded random merge differential, and merge laws checked at the mergeDB level</name>
  <files>test/app.test.js</files>
  <read_first>
    - test/app.test.js: the helpers and blocks from plan 01-03 (clone, legacyView, sameMerge, ROW_FOR, and the incident replay block)
    - index.html: mergeDB, mergeCollections, mergeUnion, mergeDateMap and mergeJournalEntry. Read only; this task changes no app code.
    - .planning/research/PITFALLS.md Pitfall 5, including the part about why array-level swaps give false results
  </read_first>
  <action>
Implements REG-13's breadth leg and REG-09's behavioural equivalence under random inputs.

1. test/app.test.js: add `mulberry32(seed)`, the standard six-line 32-bit PRNG, with SEED set to 20260911 and N set to 200.

2. Add `genDevice(rand, mode)`, starting from app.blank().
   - For each of the seven legacy lists, add 0 to 4 rows built with ROW_FOR, with tags drawn from a pool of 4 so collisions happen across devices.
     - In 'laws' mode, tags are distinct within one device.
     - In 'differential' mode, a device may hold a same-tag duplicate.
   - mtime:
     - 'differential' draws from the pool undefined, 1, 2, 2, so ties and missing mtimes occur.
     - 'laws' takes a strictly increasing global counter, so no two rows anywhere share an mtime.
   - About 1 row in 5 gets deletedAt equal to its mtime (or 1 when mtime is undefined).
   - Maps:
     - journal days come from a pool of 3 dates, each holding 1 to 3 lines from a pool of 4 strings, joined by newline.
     - mobilityLog and lawnLog days come from a pool of 3 dates, each an object of random booleans for mowed and watered. Explicit false is included, and sometimes the day is absent.
   - updatedAt:
     - 'differential' draws from a small pool that includes equal values.
     - 'laws' takes a strictly increasing counter.
   - gen:
     - 'differential' is 0 nine times in ten and 1 otherwise.
     - 'laws' is always 0.
   - draft:
     - 'differential' is null 70% of the time, a valid PUSH 1 draft 20%, and a PUSH 1 draft with no stairs 10%.
     - 'laws' is always null.
   - wx is sometimes an object.

3. Add a block headed "── a seeded random battery: legacy vs derived, and the merge laws (REG-13/REG-09) ──" after the plan 01-03 blocks.
   - Differential: for i from 0 to N-1, build A and B in 'differential' mode. For each localWins in [false, true], compare legacyView(app.mergeDB_legacy(clone(A), clone(B), lw)) with legacyView(app.mergeDB(clone(A), clone(B), lw)). Report one ok, "random differential: 400 seeded merges, legacy and derived identical", with the extra holding the first mismatching index and its differing top-level keys.
   - `contentOf(db)` returns canon of, per legacy collection:
     - lists: a map from each row's COLLECTIONS key (function or field) to canon of the row, with tombstones included
     - replace-whole maps: day to canon of the value
     - journal: day to the sorted array of distinct trimmed non-empty lines
   - Laws, over N pairs and triples in 'laws' mode, with localWins false throughout:
     - "merge law: merging a device with itself changes nothing (idempotence)": contentOf(mergeDB(A,A)) equals contentOf(A).
     - "merge law: argument order does not change the result (commutativity)": contentOf(mergeDB(A,B)) equals contentOf(mergeDB(B,A)).
     - "merge law: retried transactions converge for every list collection (associativity)": compare contentOf restricted to list collections for mergeDB(mergeDB(A,B),C) and mergeDB(A,mergeDB(B,C)).
   - Map associativity check. Build the fixed counterexample: A has updatedAt 1 and day d = {mowed:true}; B has updatedAt 3 and no d; C has updatedAt 2 and d = {mowed:false}. Assert that mergeDB_legacy and mergeDB give legacyView-equal output for both groupings. Label it "merge law: map collections are not associative today (no per-day mtime) — legacy and derived agree on the counterexample". Put a comment in the test explaining the gap. This is a pre-existing property of mergeDateMap, and this phase must not change it.
   - Every law ok's extra names the first failing index and collection only.

4. If a law fails for the derived merge and fails identically for mergeDB_legacy, that is a pre-existing property, not a derivation bug. Do not patch mergeUnion or mergeDateMap. Narrow the generator only when the counterexample is a state no real device can hold, and document why in the test. Otherwise stop and report it in the SUMMARY as a finding for Ian. If a law fails for the derived merge only, that is a derivation bug: fix mergeCollections.

5. Record in the SUMMARY, under a Findings heading, that the map collections are not associative (with the counterexample). This matters for any future sync work.

6. Commit with the subject `test(01-04): seeded random merge differential and merge-law checks`.
  </action>
  <verify>
    <automated>cd /c/Users/idbac/Projects/ppl-tracker && TZ=America/Chicago node test/app.test.js > /tmp/p0401.txt; echo "exit=$?"; grep -E 'PASS  (random differential|merge law): ' /tmp/p0401.txt; tail -1 /tmp/p0401.txt</automated>
  </verify>
  <acceptance_criteria>
    - The suite exits 0, and its last line matches `^[0-9]+ passed, 0 failed$`.
    - The output contains `PASS  random differential: 400 seeded merges, legacy and derived identical`.
    - `grep -c 'PASS  merge law: ' /tmp/p0401.txt` prints 4.
    - `grep -c 'mulberry32' test/app.test.js` prints at least 2, and `grep -c '20260911' test/app.test.js` prints at least 1.
    - `git diff HEAD~1 --stat` lists only test/app.test.js.
  </acceptance_criteria>
  <done>A seeded battery of 400 random merges proves the derived and frozen merge identical. The idempotence, commutativity and list associativity laws are checked at the mergeDB level. The map non-associativity is documented and recorded as a finding. The suite is green and the change is committed.</done>
</task>

<task type="auto">
  <name>Task 2: .gitignore plus the real-backup differential, local only and skipped loudly when absent</name>
  <files>.gitignore, test/app.test.js</files>
  <read_first>
    - .github/workflows/deploy.yml: CI runs `node test/app.test.js` with TZ set, and the Pages job uploads `path: '.'`, the whole checkout
    - test/app.test.js: the require lines and the pass/fail counters at the top, the ok() helper, and the final two lines
    - index.html: exportData (search "function exportData"). The backup is `JSON.stringify(DB)`: every collection, including the journal and notes.
    - .planning/STATE.md: the Phase 1 decision dated 2026-09-11 that the real-backup fixture stays local only
    - CLAUDE.md
  </read_first>
  <action>
Implements REG-13's real-backup leg, following Ian's decision 3 exactly.

1. Create the repo's first .gitignore at the repository root, in UTF-8 with LF line endings and a trailing newline. Start with a short comment block. It says the repo is public and the Pages deploy uploads the whole checkout, so a committed backup would publish Ian's journal, weights and notes, and that real data for the REG-13 differential lives only in test/local/. Then add exactly two rules, each on its own line: `test/local/` and `ppl-backup-*.json`. The second is the app's own export filename, so an export dropped anywhere in the repo is ignored too. Add nothing else; .gitattributes and any other repo hygiene belong to Phase 6.

2. test/app.test.js, top of the file:
   - Add `const path = require('path');` beside the existing fs require.
   - Beside the pass and fail counters, add `let skip = 0;` and a helper `skipLine(msg)`. It prints two spaces, SKIP, two spaces and the message, and increments skip.

3. test/app.test.js, last lines: change the summary console.log to print `${pass} passed, ${fail} failed, ${skip} skipped`. Leave `process.exit(fail ? 1 : 0)` unchanged. A skip never changes the exit code, so it can never block CI.

4. Add two checks that run everywhere, CI included, reading .gitignore from path.join(__dirname, '..', '.gitignore'):
   - "gitignore: test/local/ is ignored" requires a line exactly equal to `test/local/`.
   - "gitignore: exported backups (ppl-backup-*.json) are ignored" requires a line exactly equal to `ppl-backup-*.json`.

5. test/app.test.js: add a block headed "── real backup: legacy vs derived over Ian's actual data (REG-13, local only) ──" after Task 1's block. Its top comment says the block reads Ian's real journal, weights and notes. It also says an ok() extra here may only hold numbers, booleans, collection names or scenario names, never a row, a date, a note or a refusal message, because a FAIL prints its extra.
   - When REAL_PATH does not exist, call skipLine with 'real-backup differential — test/local/real-db-snapshot.json is not present (local only, git-ignored; see .gitignore)' and run nothing else in the block.
   - When it exists, read it as UTF-8 and JSON.parse it inside try/catch. Report "real backup: the file parses as JSON", false on a parse error with the extra 'unparseable', and stop the block on failure.
   - "real backup: validateBackup and its legacy twin agree" checks strict equality. The extra reports, for derived and for legacy, only null or 'refused'.
   - "real backup: it is a valid backup" checks that validateBackup returns null, with the extra 'refused' only.
   - Boot `R = loadApp(APP_PATH, <the file text>)`. "real backup: boots with every declared collection shaped" checks that nothing threw and every COLLECTIONS name has its kind's shape. The extra lists failing names only.
   - For each of the seven liveX wrappers on R: "real backup: " plus the wrapper name plus " matches its twin", comparing canon of the wrapper's output with canon of its _legacy twin's. The extra is the row count.
   - Merge scenarios. Let base be clone(R.DB), the migrated state a device actually holds. Write a helper realMerge(label, a, b, lw) whose extra lists only the differing top-level keys. Run each scenario for localWins false and true, labelled "real backup: merge parity — " plus the scenario plus " (localWins " plus lw plus ")". The scenarios:
     - self: (base, base)
     - stale copy: remote is clone(base) with mtime removed from every list row and updatedAt set to base.updatedAt minus 86400000. Run both argument orders.
     - deletions elsewhere: remote is clone(base) with every third row in each list given deletedAt and mtime of Date.now()+1. Run as (remote, base).
     - fresh device: blank() with gen equal to base.gen, against base.
     - erase: blank() with gen base.gen+1, against base.
   - Key counts (PITFALLS Pitfall 3). A LEGACY_KEY table maps sessions to app.sessKey, weights and petWeights to the row's date, cardio to app.cardioKey, ideas to app.ideaKey, todos to app.todoKey and hobbyLog to app.hobbyKey. For each name add "real backup: " plus the name plus " keeps the same number of distinct keys", comparing the distinct-key count under the COLLECTIONS key with the count under LEGACY_KEY. The extra is the key count.

6. Run the suite. Normally the executor's checkout has no test/local file, so the SKIP line must appear. If Ian has already placed the file (plan 01-06 may run before this plan finishes), the real-backup PASS lines appear instead. Either is acceptable. A FAIL is not.

7. Before committing, run `git status --porcelain` and confirm nothing under test/local/ is listed or staged. Commit with the subject `test(01-04): real-backup differential, local only and skipped loudly when absent`.
  </action>
  <verify>
    <automated>cd /c/Users/idbac/Projects/ppl-tracker && TZ=America/Chicago node test/app.test.js > /tmp/p0402.txt; echo "exit=$?"; grep -E '  SKIP  real-backup differential|PASS  gitignore: ' /tmp/p0402.txt; grep -c 'PASS  real backup: ' /tmp/p0402.txt; tail -1 /tmp/p0402.txt; git check-ignore -q test/local/real-db-snapshot.json && echo local-ignored; git check-ignore -q ppl-backup-2026-09-11.json && echo export-ignored; git check-ignore -q test/fixtures/merge-golden.json || echo golden-committable; git ls-files test/local | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The suite exits 0, and its last line matches `^[0-9]+ passed, 0 failed, [0-9]+ skipped$`.
    - Exactly one of these holds:
      - The file is absent: `grep -c '  SKIP  real-backup differential' /tmp/p0402.txt` prints 1, and the summary's skipped count is at least 1.
      - The file is present: `grep -c 'PASS  real backup: ' /tmp/p0402.txt` prints at least 25, and `grep -c 'FAIL  real backup' /tmp/p0402.txt` prints 0.
    - The output contains `PASS  gitignore: test/local/ is ignored` and `PASS  gitignore: exported backups (ppl-backup-*.json) are ignored`.
    - `git check-ignore -q test/local/real-db-snapshot.json` exits 0, and `git check-ignore -q ppl-backup-2026-09-11.json` exits 0.
    - `git check-ignore -q test/fixtures/merge-golden.json` exits 1. Plan 01-07's committed goldens are not swallowed by the rule.
    - `git ls-files test/local | wc -l` prints 0. `grep -cxF 'test/local/' .gitignore` prints 1, and `grep -cxF 'ppl-backup-*.json' .gitignore` prints 1.
  </acceptance_criteria>
  <done>The repo has a .gitignore that keeps Ian's real backup and any exported backup out of git and out of the Pages upload. The real-backup differential covers validateBackup, the boot path, all seven filters, five merge scenarios and key counts. It runs only when the local file exists, prints counts and names only, and otherwise prints a loud SKIP and a skipped count without affecting the exit code. The suite is green and the change is committed.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Ian's device → developer machine | a real export (journal, weights, notes) is copied into the working tree for local testing |
| working tree → public repo and live site | anything committed is public, and the Pages job publishes the whole checkout |
| test output → console and transcripts | a FAIL prints its `extra`; suite output gets pasted into chats and logs |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-12 | Information disclosure | Ian's real backup gets committed to the public repo or published by the Pages upload of the whole checkout | high | mitigate | .gitignore rules for test/local/ and ppl-backup-*.json. Verification with git check-ignore and `git ls-files test/local`. Committed checks assert both rules on every CI run |
| T-01-13 | Information disclosure | A FAIL in the real-backup block prints rows, dates or notes into the console or a pasted transcript | medium | mitigate | Extras are restricted to numbers, booleans and names. realMerge reports differing top-level keys only. The block comment states the rule |
| T-01-14 | Repudiation | A skipped real-data run gets reported as green | low | mitigate | A separate skip counter, a visible SKIP line, and a skipped count on the summary line. Plan 01-06 requires PASS lines, not the SKIP line |
</threat_model>

<verification>
- `cd /c/Users/idbac/Projects/ppl-tracker && TZ=America/Chicago npm test` exits 0, and the summary line reports a skipped count.
- `git check-ignore -q test/local/real-db-snapshot.json` exits 0, and `git ls-files test/local | wc -l` prints 0.
- The SUMMARY lists the map non-associativity finding under Findings.
</verification>

<success_criteria>
- ROADMAP Phase 1 success criterion 4's real-data leg is wired: the differential covers a real exported backup. It is proven on Ian's machine in plan 01-06, and the per-incident synthetic fixtures are from plan 01-03 (REG-13).
- A seeded random battery and mergeDB-level merge laws widen the equivalence proof beyond hand-picked cases (REG-09, REG-13).
- No real data can reach git or the live site, and a skipped real-data run can never pass for a green one.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Where |
|---|---|---|
| `.gitignore` (rules `test/local/`, `ppl-backup-*.json`) | new file | repository root |
| `mulberry32`, `genDevice`, `contentOf`, `SEED`, `N` | test helpers and constants | test/app.test.js |
| `skip`, `skipLine(msg)`, the `N passed, N failed, N skipped` summary | test harness counters | test/app.test.js |
| `REAL_PATH`, `realMerge`, `LEGACY_KEY` | test constant and helpers | test/app.test.js |
| `test/local/real-db-snapshot.json` | local-only, git-ignored fixture, placed by Ian in plan 01-06 and never committed | test/local/ |

## Edge coverage tally (spec-less probe fallback)

This plan authors 3 of the phase's 68 probe items into `must_haves.truths`, all explicit: REG-13 ×3 (adjacency, empty, ordering).

Phase total: 68 = 65 explicit + 1 backstop (REG-12 empty, plan 01-02) + 2 flagged assumptions (REG-14 in plan 01-07, SLEEP-02 in plan 01-05).

<output>
Create `.planning/phases/01-f1-the-collections-registry/01-04-SUMMARY.md` when done, including a Findings section that records the map-collection non-associativity counterexample.
</output>

