---
phase: 01-f1-the-collections-registry
plan: 02
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - index.html
  - test/harness.js
  - test/app.test.js
autonomous: true
requirements: [REG-06, REG-07, REG-08, REG-12, REG-13]

estimate:
  tokens: 45000
  raw_tokens: 45000
  tasks: 3
  confidence: low

must_haves:
  truths:
    - "blank() builds every collection's empty default by looping COLLECTIONS and reading only kind; every other default stays hand-written"
    - "The seven liveX() functions are one-line wrappers that call liveOf('<name>'), and liveOf is the only soft-delete filter body"
    - "validateBackup() takes its list/map and required/optional shape checks from COLLECTIONS and returns the byte-identical message the hand-written version returned for every damaged backup the suite builds"
    - "blank_legacy, liveSessions_legacy, liveWeights_legacy, livePetWeights_legacy, liveCardio_legacy, liveIdeas_legacy, liveTodos_legacy, liveHobbyLog_legacy and validateBackup_legacy still exist, renamed and byte-identical to the pre-phase bodies, and each is differential-tested against its replacement"
    - "REG-06 adjacency: every key blank_legacy() returns has a deep-equal value in blank(), and every key blank() has beyond those is a COLLECTIONS name holding its kind's empty default"
    - "REG-06 empty: blank() returns fresh containers on every call — pushing into one call's sessions, journal or hobbies leaves the next call's values empty or default"
    - "REG-06 ordering: blank() is compared as a key set, not by insertion order, and its scalar defaults (_schema SCHEMA, gen 0, unit 'lb', petName 'Freddie', routineMode '4day', draft null, lawn null, wx null, hobbySeedV2 true, exercises []) are unchanged"
    - "REG-07 adjacency: a row whose deletedAt is 0 or null counts as live, and a row with any truthy deletedAt is hidden, identically for all seven wrappers and their legacy twins"
    - "REG-07 empty: liveOf(name) returns [] when DB[name] is undefined, null or [], and skips null and non-object entries exactly as the legacy filters did"
    - "REG-07 ordering: every wrapper returns rows in stored order, and each returned element is the same object reference that DB[name] holds, so removeTodo and doneTodo still mutate the stored row"
    - "REG-07 idempotency: calling a liveX wrapper twice returns equal arrays and leaves DB[name]'s length and references unchanged"
    - "REG-07 concurrency: a softDelete between two liveOf calls is reflected on the second call, because liveOf reads DB[name] at call time and caches nothing"
    - "REG-08 adjacency: for every one of the ten pre-phase collection names and every damage (deleted, null, a string, a number, true, [] and {}) validateBackup and validateBackup_legacy return the identical string, or both return null"
    - "REG-08 empty: validateBackup({}) returns exactly the legacy missing-sessions message, and a backup whose optional collections are all absent returns null from both"
    - "REG-08 ordering: when two collections are damaged at once, the derived validator reports the same one first as the legacy validator (required lists, then optional lists, then maps)"
    - "REG-12 adjacency: no commit replaces two consumers — blank, the liveX family, validateBackup and mergeDB are each replaced in their own commit, whose subject line carries the tag 'REG-12 step N/4'"
    - statement: "REG-12 empty: every commit in this phase leaves TZ=America/Chicago npm test green, and no commit relies on a later one to pass"
      verification: backstop
    - "REG-12 ordering: git log --reverse lists the tags REG-12 step 1/4 (blank), 2/4 (liveX), 3/4 (validateBackup) and 4/4 (mergeDB, plan 01-03) in that order"
  artifacts:
    - path: "index.html"
      provides: "derived blank(), liveOf() with seven thin wrappers, derived validateBackup() shape prologue, and the renamed legacy twins"
      contains: "function liveOf(name)"
    - path: "test/app.test.js"
      provides: "LEGACY_COLLECTIONS, canon(), the blank parity, liveX differential and validateBackup string-parity blocks"
      contains: "LEGACY_COLLECTIONS"
  key_links:
    - from: "blank()"
      to: "COLLECTIONS[name].kind"
      via: "for...in loop that reads only kind"
      pattern: "for\\s*\\(\\s*const \\w+ in COLLECTIONS\\)"
    - from: "liveSessions() and the six sibling wrappers"
      to: "liveOf(name)"
      via: "one-line delegation"
      pattern: "liveOf\\('sessions'\\)"
    - from: "validateBackup() shape prologue"
      to: "COLLECTIONS required and kind"
      via: "three passes: required lists, optional lists, maps"
      pattern: "section is damaged \\(expected a list\\)"
  prohibitions:
    - statement: "MUST NOT delete blank_legacy, the seven live*_legacy twins or validateBackup_legacy in this plan; legacy functions are removed only in plan 01-07, a later commit than their replacements (REG-14)"
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT let the derived validateBackup accept any backup the hand-written one refused, or reword any refusal; import is the one place a file Ian did not write becomes his data"
      category: safety
      status: resolved
      verification: test
    - statement: "MUST NOT push to main from this plan; the replacement commits stay on the working branch and reach main only through a PR after the phase"
      category: safety
      status: resolved
      verification: judgment
---

<objective>
Replace the three cheapest hand-written consumers with registry-derived versions, one commit each and in cheapest-first order: blank() (REG-12 step 1/4), the liveX() soft-delete family (step 2/4), and validateBackup()'s shape prologue (step 3/4). Each replaced function stays in the file, renamed with a `_legacy` suffix and byte-identical. Each gets a differential test against its replacement, per REG-13.

Purpose: these consumers are pure and low-risk, so they prove the derivation pattern before mergeDB, the consumer named in the incident history, is touched in plan 01-03. Keeping the legacy twins alive is what lets plan 01-04 run the real-backup differential. They are deleted only in plan 01-07 (REG-14).

Output: derived blank, liveOf plus seven wrappers, derived validateBackup, nine legacy twins, and three differential blocks. The blocks are written to exempt collections newer than the legacy code, so adding sleep in plan 01-05 does not break them.

Constraints: the app must stay shippable at every commit. Commit subjects carry the REG-12 step tag. Do not push to main.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@.planning/phases/01-f1-the-collections-registry/01-RESEARCH.md
@.planning/phases/01-f1-the-collections-registry/01-PATTERNS.md
@.planning/phases/01-f1-the-collections-registry/01-01-SUMMARY.md
@.planning/research/PITFALLS.md
@CLAUDE.md
@index.html
@test/harness.js
@test/app.test.js

<interfaces>
From plan 01-01, already in index.html:
- `COLLECTIONS`: ten entries in the order sessions, weights, petWeights, cardio, ideas, todos, hobbyLog, journal, mobilityLog, lawnLog. Each has `kind` and `required`. Only sessions and weights have `required: true`.
- test/app.test.js has `REQUIRED_EXPORTS` (append new names), `ok(name, cond, extra)`, `app`, `loadApp(APP_PATH, seed, opts)`, and `realBackup` plus the `reject()` helper in the "── a bad backup is refused, and says why ──" block.

Pre-phase locations (they shift by roughly 30-60 lines after plan 01-01; re-locate by name):
- `function blank()` is a single object literal, pre-phase index.html:641.
- `const isLive` and the seven `liveX()` one-liners are at pre-phase index.html:651-659.
- `function validateBackup(raw)` is at pre-phase index.html:3300-3325. The hand-written shape loops are its lines 3302-3305.

Created by this plan:
- `function blank_legacy()` is the verbatim pre-phase blank.
- `function liveOf(name)` returns `(DB[name]||[]).filter(isLive)`.
- The twins `liveSessions_legacy`, `liveWeights_legacy`, `livePetWeights_legacy`, `liveCardio_legacy`, `liveIdeas_legacy`, `liveTodos_legacy` and `liveHobbyLog_legacy` are the verbatim pre-phase bodies.
- `function validateBackup_legacy(raw)` is the verbatim pre-phase validator.
- Test helpers: `LEGACY_COLLECTIONS` (the ten pre-phase names, a frozen oracle list) and `canon(v)` (JSON text with object keys sorted recursively, array order kept, and functions rendered as 'fn:' plus their name).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Derive blank() from COLLECTIONS, keeping blank_legacy (REG-12 step 1/4)</name>
  <files>index.html, test/harness.js, test/app.test.js</files>
  <read_first>
    - index.html: function blank (search "function blank()"), function load, function normalize, `let DB = load();`, and the gen-mismatch branch of mergeDB, which calls blank(). Read all of them.
    - index.html: the COLLECTIONS literal and its module-eval validation block (search "const COLLECTIONS = {")
    - test/harness.js: the `names` array
    - test/app.test.js: the REQUIRED_EXPORTS block, and the "── storage & migrations ──" block (it relies on blank and normalize)
    - .planning/phases/01-f1-the-collections-registry/01-PATTERNS.md section "blank() derivation"
  </read_first>
  <action>
Implements REG-06, REG-12 step 1/4 and REG-13 (legacy twin and differential for blank).

1. index.html: rename the existing single-literal `function blank()` to `function blank_legacy()` without changing a character of its body. Put a one-line comment above it: "FROZEN pre-registry reference for the REG-13 differential. Do not edit. Deleted in a later commit (REG-14)."

2. index.html: directly above blank_legacy, write a new `function blank()`.
   - It first builds an object holding only the hand-written scalar and non-collection defaults, with the exact values the legacy literal uses: _schema SCHEMA, gen 0, petName "Freddie", exercises [], draft null, unit "lb", routineMode "4day", hobbies HOBBIES_DEFAULT.slice(), productivity PRODUCTIVITY_DEFAULT.slice(), lawn null, wx null, hobbySeedV2 true.
   - It then runs a for...in loop over COLLECTIONS that sets out[name] to a fresh [] when COLLECTIONS[name].kind is 'list' and a fresh {} otherwise, and returns out.
   - The loop may read `kind` and nothing else from the registry. blank runs during `let DB = load()`, and narrowing what it reads at module-eval time is the TDZ rule in 01-RESEARCH.md.
   - Add a one-line comment saying so.

3. test/harness.js: append 'blank_legacy' to `names`. test/app.test.js: append 'blank_legacy' to REQUIRED_EXPORTS.

4. test/app.test.js, above the new block: add the helper `canon(v)`, which returns JSON text with object keys sorted recursively, array order kept, and functions rendered as 'fn:' plus their name. Also add the constant `LEGACY_COLLECTIONS`, listing the ten pre-phase collection names. Add a comment on it: "the legacy functions know exactly these ten; a collection declared later is exempt from legacy comparisons by construction, and is covered by its own tests".

5. test/app.test.js: add a block headed "── blank() is derived from COLLECTIONS (REG-06) ──", placed right after the "── storage & migrations ──" block. Add these oks:
   - "blank: every key the hand-written blank() had is unchanged" compares canon values per key of blank_legacy(). The extra lists only the differing key names.
   - "blank: every extra key is a declared collection holding its kind's empty default"
   - "blank: every declared collection is present in its kind's shape"
   - "blank: returns fresh containers on every call": push into the first call's sessions, journal and hobbies, then check that a second call's sessions is empty, its journal has no keys, and its hobbies length equals HOBBIES_DEFAULT's length.
   - "blank: scalar defaults unchanged" checks the eleven scalar values listed in step 2.
   - "blank: reads only kind from the registry": `app.blank.toString()` with block and line comments stripped first, mentions COLLECTIONS and kind, and has no property access named key, sortBy, merge, columns or format.

6. Run the suite. Every existing check, including the migration, merge and smoke blocks that call blank indirectly, stays green.

7. Commit with the subject `refactor(01-02): derive blank() from COLLECTIONS — REG-12 step 1/4`.
  </action>
  <verify>
    <automated>cd /c/Users/idbac/Projects/ppl-tracker && TZ=America/Chicago node test/app.test.js > /tmp/p0201.txt; echo "exit=$?"; grep -c 'PASS  blank: ' /tmp/p0201.txt; tail -1 /tmp/p0201.txt; git log -1 --format=%s</automated>
  </verify>
  <acceptance_criteria>
    - The suite exits 0, and its last line matches `^[0-9]+ passed, 0 failed$`.
    - `grep -c 'PASS  blank: ' /tmp/p0201.txt` prints 6.
    - The output still contains `PASS  a fresh install still boots clean` and `PASS  migrations are idempotent`.
    - `grep -c '^function blank_legacy()' index.html` prints 1. `grep -c '^function blank()' index.html` prints 1.
    - `git log -1 --format=%s` contains `REG-12 step 1/4`.
  </acceptance_criteria>
  <done>blank() is derived from COLLECTIONS and reads only kind. blank_legacy is kept verbatim. The parity block proves the two are equal on every legacy key and that any extra key is a declared collection. The suite is green and the change is committed as step 1/4.</done>
</task>

<task type="auto">
  <name>Task 2: Derive the liveX() family from COLLECTIONS via liveOf(), keeping the seven twins (REG-12 step 2/4)</name>
  <files>index.html, test/harness.js, test/app.test.js</files>
  <read_first>
    - index.html: the soft-delete section. Search "const isLive" and read through pruneDeleted. That covers isLive, touch, the seven liveX one-liners, softDelete and softRestore.
    - index.html: removeTodo and doneTodo (pre-phase lines 1274-1275). They mutate the row object returned by liveTodos()[i], which is why reference identity matters.
    - index.html: the COLLECTIONS literal (the kind and soft fields)
    - test/harness.js: the `names` array
    - test/app.test.js: the blank block added in Task 1, and the populatedDB() fixture (it seeds a soft-deleted session)
    - .planning/phases/01-f1-the-collections-registry/01-PATTERNS.md section "liveOf() + liveX() wrappers"
    - .planning/research/PITFALLS.md, Pitfall 6
  </read_first>
  <action>
Implements REG-07, REG-12 step 2/4 and REG-13 (twins and differential for the filter family).

1. index.html: directly above liveSessions, add `function liveOf(name)`.
   - It looks up COLLECTIONS[name]. When the entry is missing, is not kind 'list', or has soft not true, it throws a new Error with the message "liveOf: " plus the name plus " is not a declared soft-delete list". A new soft list therefore gets a working filter from its declaration alone, and a misuse fails loudly.
   - Otherwise it returns `(DB[name]||[]).filter(isLive)`.
   - Add a comment: this is the only soft-delete filter body. Every named wrapper delegates to it. Never generate wrappers dynamically through window or globalThis, because that breaks grep and go-to-definition (01-RESEARCH.md, Anti-Patterns).

2. index.html: copy each of the seven existing wrappers verbatim into a twin whose name adds the suffix _legacy: liveSessions, liveWeights, livePetWeights, liveCardio, liveIdeas, liveTodos, liveHobbyLog. Place the seven twins together directly below the wrappers under one comment: "FROZEN pre-registry filters for the REG-13 differential. Do not edit. Deleted in a later commit (REG-14)."

3. index.html: rewrite each of the seven wrappers as a one-liner that returns liveOf of its collection name as a string literal: 'sessions', 'weights', 'petWeights', 'cardio', 'ideas', 'todos', 'hobbyLog'. Keep the wrapper names, their order and every call site unchanged.

4. test/harness.js `names` and test/app.test.js REQUIRED_EXPORTS: append 'liveOf', 'liveSessions', 'liveCardio', 'liveIdeas', 'liveTodos', 'liveHobbyLog' and 'softDelete'. liveWeights and livePetWeights are already exported. Also append the seven twin names.

5. test/app.test.js: add a block headed "── the soft-delete filters are derived from COLLECTIONS (REG-07) ──" directly after the blank block. Use a fresh instance from `loadApp(APP_PATH)` and set its DB to a blank()-based database. In each of the seven lists, put six entries in this order: a live row {n:1}, a row with deletedAt 5 and mtime 5, a row with deletedAt 0, a row with deletedAt null, a null entry, and the string 'junk'. Add these oks:
   - "live: " plus the wrapper name plus " matches its hand-written twin", seven times, comparing canon of the two outputs.
   - "live: " plus the wrapper name plus " returns the stored rows, not copies", seven times: every derived element is strictly the same reference as the twin's element at that index.
   - "live: a missing, null or empty collection gives []": with cardio undefined, ideas null and todos [], each wrapper returns [] and equals its twin.
   - "live: calling a filter twice changes nothing": two liveSessions calls are canon-equal, and DB.sessions length is unchanged.
   - "live: a delete between two reads shows on the next read": softDelete the live weights row between two liveWeights calls, and the count drops by one.
   - "live: soft collection " plus name plus " hides a deleted row", one for every COLLECTIONS entry with kind 'list' and soft true. It sets DB[name] to one live row and one deleted row and expects liveOf(name) to have length 1. The loop iterates COLLECTIONS, so a collection declared later is covered without a test edit (Pitfall 6).
   - "live: liveOf refuses a name that is not a declared soft list": liveOf('journal') and liveOf('nope') both throw.
   - "live: every wrapper delegates to liveOf": each wrapper's toString contains liveOf with its own quoted name.
   - "live: no dynamically generated filters": `app.__src` has no bracket assignment on window or globalThis whose key starts with live.

6. Run the suite. All smoke checks stay green, including the check that the soft-deleted session in populatedDB never renders.

7. Commit with the subject `refactor(01-02): derive the liveX() filters from COLLECTIONS — REG-12 step 2/4`.
  </action>
  <verify>
    <automated>cd /c/Users/idbac/Projects/ppl-tracker && TZ=America/Chicago node test/app.test.js > /tmp/p0202.txt; echo "exit=$?"; grep -c 'PASS  live: ' /tmp/p0202.txt; tail -1 /tmp/p0202.txt; git log -1 --format=%s</automated>
  </verify>
  <acceptance_criteria>
    - The suite exits 0, and its last line matches `^[0-9]+ passed, 0 failed$`.
    - `grep -c 'PASS  live: ' /tmp/p0202.txt` prints at least 20.
    - The output contains `PASS  live: liveTodos returns the stored rows, not copies` and `PASS  live: soft collection hobbyLog hides a deleted row`.
    - `grep -c '^function liveOf(name)' index.html` prints 1. `grep -c "return liveOf('" index.html` prints at least 7.
    - `git log -1 --format=%s` contains `REG-12 step 2/4`.
  </acceptance_criteria>
  <done>All seven liveX wrappers delegate to liveOf, which is derived from COLLECTIONS and refuses undeclared names. The seven twins are kept verbatim and match the derived filters output-for-output and reference-for-reference. Every soft list in the registry is checked by a loop. The suite is green and the change is committed as step 2/4.</done>
</task>

<task type="auto">
  <name>Task 3: Derive validateBackup()'s shape checks from COLLECTIONS, keeping validateBackup_legacy (REG-12 step 3/4)</name>
  <files>index.html, test/harness.js, test/app.test.js</files>
  <read_first>
    - index.html: the whole of function validateBackup (search "function validateBackup(raw)"), plus importData, which calls it and relies on it returning null or a reason string
    - index.html: the COLLECTIONS literal (its kind and required fields)
    - test/app.test.js: the "── a bad backup is refused, and says why ──" block, including realBackup and the reject() helper
    - .planning/phases/01-f1-the-collections-registry/01-PATTERNS.md section "validateBackup() shape prologue"
    - .planning/research/PITFALLS.md Pitfall 11, and the Out of Scope table in .planning/REQUIREMENTS.md (field-level validators are rejected)
  </read_first>
  <action>
Implements REG-08, REG-12 step 3/4 and REG-13 (the twin and differential for import validation).

1. index.html: copy the entire current validateBackup function verbatim into a new `function validateBackup_legacy(raw)`. Place it directly below validateBackup with the comment "FROZEN pre-registry validator for the REG-13 differential. Do not edit. Deleted in a later commit (REG-14)."

2. index.html: in validateBackup, replace only the four hand-written shape lines: the arrays constant naming the two required lists and its loop, the loop over the five optional list names, and the loop over the three map names. Put three passes over COLLECTIONS in their place, in this order.
   - Pass 1 covers every entry with kind 'list' and required true. When raw[name] is not an array, return the legacy missing-list message with the name substituted.
   - Pass 2 covers every entry with kind 'list' and required false. When raw[name] is neither null nor undefined and is not an array, return the legacy "section is damaged (expected a list)" message with the name substituted.
   - Pass 3 covers every entry with kind 'map'. When raw[name] is neither null nor undefined and is not a non-array object, return the legacy "section is damaged" message with the name substituted.
   Keeping three passes reproduces the legacy precedence whatever order COLLECTIONS is declared in. The three message templates must be byte-identical to the legacy ones, including the U+2019 right single quotation mark in the missing-list message, so copy the template text from the legacy lines rather than retyping it. Leave the first non-object check, the _schema checks and every per-row check for workouts and weigh-ins byte-identical. The deep checks stay hand-written. There is no validation DSL (Pitfall 11 and the Out of Scope table).

3. test/harness.js `names` and test/app.test.js REQUIRED_EXPORTS: append 'validateBackup_legacy'.

4. test/app.test.js: add a block headed "── validateBackup() takes its shape checks from COLLECTIONS (REG-08) ──" directly after the existing "── a bad backup is refused, and says why ──" block. Reuse that block's realBackup, and deep-clone it for every case. Add these checks:
   - Named battery. Take every mutation the existing reject() calls use, plus cardio set to the string 'x', mobilityLog set to [] and lawnLog set to the number 3. For each one add an ok labelled "validate: same message — " plus a short label, asserting strict equality between validateBackup(copy) and validateBackup_legacy(copy). Put both strings in the extra; they come from synthetic data.
   - Generated battery. For every name in LEGACY_COLLECTIONS, crossed with each damage (property deleted, null, 'x', 42, true, [] and {}), compare the two validators. Report it as one ok labelled "validate: every legacy collection × every damage gives the same answer", with the extra holding the case count and the first mismatching name and damage.
   - "validate: two faults report the same one first" covers the pairs (cardio 'x' with journal []), (sessions deleted with petWeights 'x') and (todos {} with lawnLog []).
   - "validate: an empty object is refused with the missing-sessions message" asserts the two outputs for {} are equal and that the message contains 'sessions'.
   - "validate: a backup with no optional collections is accepted by both": realBackup with every non-required collection deleted gives null from both.
   - "validate: a non-object is refused identically" covers [1,2,3], null, 'str' and 5.
   - "validate: shape lists come from COLLECTIONS, per-row checks stay hand-written": validateBackup's toString contains COLLECTIONS, 'Workout ' and 'Weigh-in on', and does not contain the hand-typed quoted list that begins with 'petWeights','cardio'.

5. Run the suite. The existing "a bad backup is refused" checks stay green unmodified.

6. Commit with the subject `refactor(01-02): derive validateBackup()'s shape checks from COLLECTIONS — REG-12 step 3/4`.
  </action>
  <verify>
    <automated>cd /c/Users/idbac/Projects/ppl-tracker && TZ=America/Chicago node test/app.test.js > /tmp/p0203.txt; echo "exit=$?"; grep -c 'PASS  validate: ' /tmp/p0203.txt; tail -1 /tmp/p0203.txt; git log --reverse --format=%s | grep -o 'REG-12 step [0-9]/4'</automated>
  </verify>
  <acceptance_criteria>
    - The suite exits 0, and its last line matches `^[0-9]+ passed, 0 failed$`.
    - `grep -c 'PASS  validate: ' /tmp/p0203.txt` prints at least 17.
    - The output still contains `PASS  a good backup passes` and `PASS  a damaged journal is refused`.
    - `grep -c 'so it’s incomplete or truncated' index.html` prints 2, one in validateBackup and one in validateBackup_legacy.
    - `grep -c '^function validateBackup_legacy(raw)' index.html` prints 1.
    - `git log --reverse --format=%s | grep -o 'REG-12 step [0-9]/4'` prints `REG-12 step 1/4`, `REG-12 step 2/4` and `REG-12 step 3/4`, in that order.
  </acceptance_criteria>
  <done>validateBackup's list/map and required/optional shape checks come from COLLECTIONS in three precedence-preserving passes. The per-row checks are untouched. validateBackup_legacy is kept verbatim and returns the identical string on every named, generated and two-fault case. The suite is green and the change is committed as step 3/4.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| imported file → DB | a backup file Ian did not write, possibly hand-edited or truncated, reaches `validateBackup()` and then `normalize()` / `mergeDB()` |
| stored rows → views | soft-deleted rows stay in the blob by design and must be filtered on every read |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-05 | Tampering | The derived validateBackup accepts a malformed backup that the hand-written one refused (ASVS V5, input validation) | high | mitigate | Task 3's string-identical differential over the named battery, a generated battery of 10 collections × 7 damages, two-fault precedence cases and non-object inputs. The per-row checks are left byte-identical |
| T-01-06 | Tampering | liveOf returns copies or skips the filter. A delete through liveTodos()[i] then silently does nothing, or a deleted row ghosts back into a view | medium | mitigate | Task 2's per-wrapper reference-identity checks, the loop over every soft collection, and the existing smoke check that the soft-deleted session never renders |
| T-01-07 | Tampering | The derived blank() shares one container across calls, so wipe() and mergeDB's blank() base would alias state | medium | mitigate | Task 1's "fresh containers on every call" check |
</threat_model>

<verification>
- `cd /c/Users/idbac/Projects/ppl-tracker && TZ=America/Chicago npm test` exits 0.
- `git log --reverse --format=%s | grep -o 'REG-12 step [0-9]/4'` shows steps 1/4, 2/4 and 3/4 in order, each in its own commit.
- `grep -cE '^function (blank_legacy|validateBackup_legacy)\(' index.html` prints 2, and `grep -cE '^function live[A-Za-z]+_legacy\(\)' index.html` prints 7. The legacy twins still exist for plans 01-03 and 01-04.
</verification>

<success_criteria>
- blank(), the liveX() family and validateBackup() are derived from COLLECTIONS (REG-06, REG-07, REG-08). Each is in its own commit, cheapest first (REG-12).
- Every replaced function is kept, renamed, and differential-tested against its replacement over synthetic fixtures (REG-13). Plan 01-04 adds the real-backup leg.
- Nothing an import used to refuse is accepted now, and no refusal changed its wording.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Where |
|---|---|---|
| `blank()` (derived) and `blank_legacy()` (frozen) | function declarations | index.html, pre-phase blank() position |
| `liveOf(name)` | function declaration | index.html, soft-delete section |
| `liveSessions`, `liveWeights`, `livePetWeights`, `liveCardio`, `liveIdeas`, `liveTodos`, `liveHobbyLog` (rewritten as one-line wrappers) | function declarations | index.html, soft-delete section |
| `liveSessions_legacy`, `liveWeights_legacy`, `livePetWeights_legacy`, `liveCardio_legacy`, `liveIdeas_legacy`, `liveTodos_legacy`, `liveHobbyLog_legacy` | function declarations (frozen) | index.html, directly below the wrappers |
| `validateBackup()` (derived prologue) and `validateBackup_legacy()` (frozen) | function declarations | index.html, backup section |
| `LEGACY_COLLECTIONS`, `canon(v)` | test constant and helper | test/app.test.js |

## Edge coverage tally (spec-less probe fallback)

This plan authors 14 of the phase's 68 probe items into `must_haves.truths`: REG-06 ×3, REG-07 ×5, REG-08 ×3 and REG-12 ×3. Thirteen are explicit truths. One, REG-12 empty ("every commit leaves the suite green"), is a structured `verification: backstop` marker, because proving it means checking out and running every commit.

Phase total: 68 = 65 explicit + 1 backstop + 2 flagged assumptions (REG-14 in plan 01-07, SLEEP-02 in plan 01-05).

<output>
Create `.planning/phases/01-f1-the-collections-registry/01-02-SUMMARY.md` when done.
</output>
