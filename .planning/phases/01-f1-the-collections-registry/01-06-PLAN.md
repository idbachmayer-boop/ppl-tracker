---
phase: 01-f1-the-collections-registry
plan: 06
type: execute
wave: 5
depends_on: [01-04]
files_modified: []
autonomous: false
requirements: [REG-13]

estimate:
  tokens: 15000
  raw_tokens: 15000
  tasks: 2
  confidence: low

must_haves:
  truths:
    - "Ian's real exported backup is at test/local/real-db-snapshot.json on the development machine, is ignored by git, and is not tracked"
    - "With that file in place, the full suite prints the real-backup PASS lines instead of the SKIP line, with zero failures"
    - "01-06-SUMMARY.md records the line 'Real-backup differential: PASS' with file size, _schema, per-collection row counts and the number of real-backup checks passed — no row content, dates, notes or exercise names"
  artifacts:
    - path: "test/local/real-db-snapshot.json"
      provides: "Ian's real exported backup, local only, git-ignored and never committed"
    - path: ".planning/phases/01-f1-the-collections-registry/01-06-SUMMARY.md"
      provides: "the counts-only record that the real-data differential passed, which plan 01-07's precondition reads"
      contains: "Real-backup differential: PASS"
  key_links:
    - from: "test/app.test.js real-backup block (plan 01-04)"
      to: "test/local/real-db-snapshot.json"
      via: "fs.existsSync guard; SKIP when absent"
      pattern: "real-db-snapshot\\.json"
    - from: "plan 01-07 Task 2 precondition"
      to: "01-06-SUMMARY.md"
      via: "the literal line 'Real-backup differential: PASS'"
      pattern: "Real-backup differential: PASS"
  prohibitions:
    - statement: "MUST NOT commit, paste, summarise or quote the real backup's contents anywhere (SUMMARY, commit message, chat); only sizes and counts are recorded"
      category: privacy
      status: resolved
      verification: test
    - statement: "MUST NOT record the real-backup differential as passed unless its PASS lines actually ran; a SKIP line means this checkpoint is not done"
      category: transparency
      status: resolved
      verification: test
    - statement: "MUST NOT fabricate, synthesise or substitute a stand-in for the real backup (Ian's decision 3); if Ian cannot export one now, the plan stays paused and plan 01-07 waits"
      category: transparency
      status: resolved
      verification: judgment
---

<objective>
Put Ian's real exported backup in place, locally only, and prove the REG-13 real-data differential passes on it.

REG-13 needs a real exported backup as well as the synthetic fixtures, because a green synthetic suite only proves the derived code does not repeat the documented incidents. It says nothing about the long tail of real data (PITFALLS Pitfall 4).

Per Ian's decision 3 (2026-09-11), this is a `checkpoint:human-action`. Only Ian can export from his phone. The file is never fabricated, and it stays in the git-ignored test/local/ that plan 01-04 set up.

This plan changes no code. It gates plan 01-07's deletion of the legacy functions: they may be deleted only after the real data has been run through them.

Output: the local file (never committed) and a counts-only SUMMARY carrying the line `Real-backup differential: PASS`.

This plan may run any time after plan 01-04, in parallel with plan 01-05, because they share no files. Do not push to main.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/01-f1-the-collections-registry/01-04-SUMMARY.md
@.gitignore
@test/app.test.js
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Ian exports a real backup from the live app and places it in the local-only folder</name>
  <read_first>
    - .gitignore (from plan 01-04), which must list test/local/
    - test/app.test.js, the "── real backup: legacy vs derived over Ian's actual data (REG-13, local only) ──" block, to confirm the path it reads
  </read_first>
  <action>Ask Ian to do the following. Only he can, because the data lives on his phone. Present it in plain words.

1. On the phone, open the live PPL Tracker (https://idbachmayer-boop.github.io/ppl-tracker/), go to Settings, and tap "Export backup (.json)". The phone saves a file named like ppl-backup-2026-09-11.json.
2. Move that file to this PC over a private channel you already use. It holds your journal, weigh-ins and notes.
3. Create the folder C:\Users\idbac\Projects\ppl-tracker\test\local\ and save the file there under the name real-db-snapshot.json.
4. Do not open or paste it in a chat, and do not commit it. Git already ignores that folder, and ignoring it is also what keeps it off the public site.

If Ian cannot do this now, the plan stays paused here. Plan 01-07's legacy deletion is blocked until this completes. Never create a substitute file.</action>
  <verify>
    <automated>cd /c/Users/idbac/Projects/ppl-tracker && test -f test/local/real-db-snapshot.json && git check-ignore -q test/local/real-db-snapshot.json && echo "placed and ignored"</automated>
  </verify>
  <acceptance_criteria>
    - `test -f test/local/real-db-snapshot.json` exits 0.
    - `git check-ignore -q test/local/real-db-snapshot.json` exits 0.
    - `git ls-files test/local | wc -l` prints 0.
  </acceptance_criteria>
  <instructions>
    1. On your phone, open https://idbachmayer-boop.github.io/ppl-tracker/, go to Settings, and tap "Export backup (.json)".
    2. Send the downloaded ppl-backup-YYYY-MM-DD.json to this PC over a private channel you already use.
    3. Save it as C:\Users\idbac\Projects\ppl-tracker\test\local\real-db-snapshot.json, creating the test\local folder if it isn't there.
    4. Don't open it in a chat, and don't commit it. Git ignores that folder.
  </instructions>
  <verification>
    After Ian resumes with "placed", Claude checks with read-only commands:
    - `test -f test/local/real-db-snapshot.json` exits 0
    - `git check-ignore -q test/local/real-db-snapshot.json` exits 0
    - `git ls-files test/local` prints nothing
    Claude never prints the file.
  </verification>
  <resume-signal>Type "placed" once the file is at test\local\real-db-snapshot.json, or "not now" to leave the phase paused at this checkpoint.</resume-signal>
  <done>The real backup is at test/local/real-db-snapshot.json, ignored by git and not tracked.</done>
</task>

<task type="auto">
  <name>Task 2: Run the real-backup differential and record the result, counts only</name>
  <precondition>test/local/real-db-snapshot.json exists and `git check-ignore -q test/local/real-db-snapshot.json` exits 0 (Task 1 resumed with "placed")</precondition>
  <files>test/local/real-db-snapshot.json (read only, git-ignored)</files>
  <read_first>
    - test/app.test.js, the real-backup block and the summary line
    - .gitignore
    - .planning/phases/01-f1-the-collections-registry/01-04-SUMMARY.md
  </read_first>
  <action>
Implements REG-13's real-data proof.

1. Check the precondition with read-only commands:
   - the file exists
   - `git check-ignore -q` exits 0
   - `git ls-files test/local` prints nothing
   - `git status --porcelain` lists nothing under test/local

2. Confirm the file is a backup without printing any of it. Use one `node -e` command that parses the file and prints only its size in bytes, its _schema value, and each collection name with its row count: array length for lists, key count for maps. Print nothing else.

3. Run `TZ=America/Chicago node test/app.test.js`. It passes only when all of these hold:
   - no line starting with two spaces, SKIP and real-backup differential
   - at least 25 lines starting "PASS  real backup: "
   - 0 failed

4. If any "FAIL  real backup" line appears, stop. Change no code in this plan. Record in the SUMMARY the labels of the failing checks, which name only collections and scenarios, and return them as a gap for `/gsd-plan-phase 1 --gaps`. Plan 01-07 must not start until a gap plan makes this pass.

5. Write the result into 01-06-SUMMARY.md:
   - the literal line "Real-backup differential: PASS", or "FAIL" with the labels
   - the date
   - the file size in bytes and the _schema
   - per-collection row counts
   - the number of real-backup PASS lines
   - the suite's summary line
   Never include a row, a date range, a note, a journal line, an exercise name or a refusal message.

6. Commit nothing from test/local. The file stays local forever.
  </action>
  <verify>
    <automated>cd /c/Users/idbac/Projects/ppl-tracker && test -f test/local/real-db-snapshot.json && git check-ignore -q test/local/real-db-snapshot.json && TZ=America/Chicago node test/app.test.js > /tmp/p0602.txt; echo "exit=$?"; grep -c '  SKIP  real-backup differential' /tmp/p0602.txt; grep -c 'PASS  real backup: ' /tmp/p0602.txt; grep -c 'FAIL' /tmp/p0602.txt; tail -1 /tmp/p0602.txt; git ls-files test/local | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - The suite exits 0, and its last line matches `^[0-9]+ passed, 0 failed, 0 skipped$`.
    - `grep -c '  SKIP  real-backup differential' /tmp/p0602.txt` prints 0, `grep -c 'PASS  real backup: ' /tmp/p0602.txt` prints at least 25, and `grep -c 'FAIL' /tmp/p0602.txt` prints 0.
    - `git ls-files test/local | wc -l` prints 0.
    - `grep -c 'Real-backup differential: PASS' .planning/phases/01-f1-the-collections-registry/01-06-SUMMARY.md` prints 1.
    - `grep -cE '"(note|text|entry|name)"\s*:' .planning/phases/01-f1-the-collections-registry/01-06-SUMMARY.md` prints 0. No row content was pasted.
  </acceptance_criteria>
  <done>The real-data differential ran over Ian's actual backup with every check passing and nothing skipped. The SUMMARY records the pass with counts only. The file remains local and untracked.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| phone → development machine | Ian's full export (journal, weigh-ins, notes) is copied onto the PC |
| verification output → planning docs and chat | whatever the executor prints or writes can end up in a committed SUMMARY or a transcript |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-01-19 | Information disclosure | Backup contents pasted into the SUMMARY, a commit message or the chat while verifying | high | mitigate | Counts-only protocol. The node check prints lengths only. The SUMMARY acceptance check greps for row fields. Plan 01-04's block prints only counts and names |
| T-01-20 | Information disclosure | The backup file is sent between devices over an insecure channel | low | accept | Single user, his own data. Ian chooses a private channel he already uses, and the plan asks for one |
</threat_model>

<verification>
- `git ls-files test/local | wc -l` prints 0, and `git check-ignore -q test/local/real-db-snapshot.json` exits 0.
- The suite's summary line shows 0 failed and 0 skipped, with at least 25 real-backup PASS lines.
</verification>

<success_criteria>
- REG-13's "real exported backup" leg is proven on Ian's machine, alongside the per-incident synthetic fixtures (plan 01-03) and the seeded battery (plan 01-04). ROADMAP success criterion 4's precondition for deleting the legacy code is met.
- The real data never left the git-ignored local folder and was never quoted.
</success_criteria>

## Artifacts this phase produces (this plan's share)

| Symbol / file | Kind | Where |
|---|---|---|
| `test/local/real-db-snapshot.json` | local-only fixture, placed by Ian and never committed | test/local/ |
| "Real-backup differential: PASS" record | counts-only SUMMARY line, read by plan 01-07's precondition | 01-06-SUMMARY.md |

No new code symbols.

## Edge coverage tally (spec-less probe fallback)

This plan authors none of the 68 probe items. REG-13's three edge items are in plan 01-04.

Phase total: 68 = 65 explicit + 1 backstop (REG-12 empty, plan 01-02) + 2 flagged assumptions (REG-14 in plan 01-07, SLEEP-02 in plan 01-05).

<output>
Create `.planning/phases/01-f1-the-collections-registry/01-06-SUMMARY.md` when done, counts only.
</output>
