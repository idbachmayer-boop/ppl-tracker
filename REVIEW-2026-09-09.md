# Review — 2026-09-09

A full pass over code, logic, design and security, done at Ian's request with one goal above the
others: **a foundation that makes it cheap and safe for future agents to add things.**

## What this document is, and is not

It is a **review**. It is not a GSD codebase map, and it must not be treated as one.

`.planning/codebase/` is produced by `/gsd-map-codebase`, and the mere presence of those files is
what tells `/gsd-onboard` that mapping is done. Writing them by hand would make GSD skip its real
mapping pass and build the roadmap on one reviewer's summary instead — the opposite of a trustworthy
foundation. So run `/gsd-map-codebase` for real. Read this first if you like; it will make that pass
faster, and it names the things worth looking at hardest. It is not a substitute for it.

**Read `CLAUDE.md` before anything here.** It is authoritative and it wins on conflict.

## What was read

`index.html` (4,131 lines), `sw.js`, `test/app.test.js`, `test/harness.js`, `CLAUDE.md`, `README.md`,
`package.json`, `.github/workflows/deploy.yml`, git history, and the app's note in Ian's vault. The
suite was run: green before and after the changes below.

## Already fixed on this branch

Two commits, each green in isolation.

**`2303c31` — security rules into git, changelog pointed at the live vault.**
`firestore.rules` now carries a reviewable copy of the data-protection layer. **It has not been
reconciled with what is deployed** — read the header of that file before touching Firebase.
Separately, `CLAUDE.md` had been telling every agent to write the changelog into the *pre-rebuild*
vault, a directory Ian no longer opens. The write succeeded every time, so nothing ever reported it.

**`e98b957` — a malformed draft no longer kills the Log tab, and every screen is now smoke-tested.**
See below; this one is worth understanding because it is the shape of the whole problem.

## The bug that matters, and why

`viewActive` walks `DB.draft.entries`, `.stairs` and `.extras` without checking any of them, because
`startWorkout()` always builds all three. A draft that arrived any other way — synced from a device
on an older schema, or restored from a hand-edited backup — can be short a field, and a missing
`stairs` throws. The screen it takes out is the Log tab: the one Ian is standing in front of at the
rack.

This was not a new hole. The guard immediately above it in `mergeDB` exists for exactly this reason;
its own comment says a bad `workout` name "would crash viewActive's `PROGRAM[name]` lookup." It was
one field short, and had been for months.

It was found by the new smoke check **on its first run**, which is the point of adding one.

Note the second-order cause: `render()` catches whatever a view throws and shows a friendly
"Something broke on this screen" card. That is right for Ian mid-workout and wrong for the suite —
until this branch, an agent could break `viewHistory()` outright and all 187 checks would still pass.
The failure would surface on the phone.

## Still open — security

**Open signup.** `createUserWithEmailAndPassword` is live on a public site with a public repo, so
anyone can create an account in the Firebase project. The rules isolate their data, but it is Ian's
quota, his billing, and an abuse surface for a feature exactly two people need. **Fix in the console,
not in code:** Authentication → Settings → disable new sign-ups once both accounts exist. App Check
is the heavier alternative.

**Rules not reconciled.** Nobody has looked at what is actually deployed. Diff `firestore.rules`
against the console before deploying it.

**No CSP.** Blocked by inline `onclick` plus the inline `<script>`. Chicken-and-egg with F2 below —
doing the event-delegation work is what makes a CSP possible.

**Escaping has a second hole.** The stated convention is "every user string goes through `esc()`",
and `esc()` deliberately does not escape `'`. But values interpolated into an *attribute*
(`value="${st.w}"` in the set rows) are not escaped at all, and `validateBackup()` checks the
*shape* of an imported file, never the types inside it — a hand-edited backup can put anything in a
set's `w`. Low severity today (only Ian imports backups) but the convention claims cover it does not
have, and the next agent will trust the convention.

**CI publishes the whole repo.** `path: '.'` in `deploy.yml` uploads `test/`, `CLAUDE.md` and this
file to the live site. Nothing leaks — the repo is public — but it is noise on the served surface.

## Still open — foundation

### F1. The schema is written in five places and nowhere *(highest value)*

To know what a "session" is you must read `blank()`, `MIGRATIONS`, `mergeDB()`, `validateBackup()`
and the `liveSessions()` filter. Adding one new tracked thing means editing all five, correctly.

Look at the bug history in `CLAUDE.md`. It is all one bug:

- `mobilityLog` — merge rule forgotten, absence read as "off"
- Migration 15 — `touch()` and persist forgotten, silently reverted in production
- Soft delete — "a missed filter ghosts a deleted row back into a view"
- The draft crash above — a field the views assume, unguarded on every path but one

Those are not four bugs. They are one: **the rules for a collection are implicit, so each new
collection re-learns them by failing in production.**

The fix is a single declaration the other five places derive from:

```js
const COLLECTIONS = {
  sessions:    { kind:'list', key:'id', soft:true,  sortBy:'date' },
  weights:     { kind:'list', key:'id', soft:true,  sortBy:'date' },
  mobilityLog: { kind:'map',  merge:'newer-wins', explicitFalse:true },
  lawnLog:     { kind:'map',  merge:'newer-wins', explicitFalse:true },
  // ...
};
```

Adding a feature becomes one line instead of five edits, and the merge rule cannot be forgotten
because there is nowhere left to forget it. **Constraint:** anything reached from `normalize()` runs
at module-eval time, so it must not touch a `const` declared further down — that temporal-dead-zone
trap has already taken out the boot once and is warned about twice in the file.

### F2. Inline `onclick` forces 326 global functions

Every handler must be a `window` global because the markup calls it by name. No namespacing, silent
collisions, no CSP possible, and it is the root of the apostrophe footgun. One delegated listener on
`#app` reading `data-act` / `data-arg` removes all of it at once. Mechanical, testable, and it needs
no build step.

### F3. `CLAUDE.md` gives the war stories but not the recipe

It says what breaks and why, which is genuinely excellent. It does not say *"to add a new tracked
thing, do these six steps in this order."* An agent following a numbered recipe beats an agent
inferring one from incident reports.

### F4. Line endings are unmanaged

`index.html` is CRLF on disk, the tests are LF, there is no `.gitattributes`, and `core.autocrlf` is
rewriting them on every touch. Adding `* -text` would make git store bytes exactly — and would
produce a 4,131-line diff on the next commit of `index.html`. That is a deliberate decision, not
something to slip into a cleanup commit. **Left untouched on purpose.**

### F5. The single-file rule should stay

330KB is a lot to hold in context, but the file is genuinely well-sectioned, and splitting it means a
build step, which is the thing that makes the app editable without a toolchain. F1–F3 buy more than
splitting would. Recommendation: leave it.

## Questions for Ian — ANSWERED 2026-09-09

All six were put to Ian and answered the same day. **These are decisions, not suggestions.** Do not
re-open them during planning; if one turns out to be wrong, say so explicitly rather than quietly
planning around it.

**1. Is cloud sync worth Firebase?** → **Leave sync exactly as it is. Do not rewrite it.**
Ian uses the app on his **phone only**; the cloud is "nice, but not super necessary." The obvious
inference is to strip Firebase out — that is wrong, and the first draft of this document leaned that
way before weighing what the cloud is also doing. It is the **only automatic off-device backup**: when
the phone dies, localStorage dies with it. An intermediate idea (demote to one-way backup, delete the
merge) was also considered and dropped: the sync code works, is tested, and rewriting it is real data
risk for near-zero payoff on a single device. Signup being closed removes the actual live risk.
**Decision: no sync work. Revisit only if it causes a problem.**

**2. `draft` is a single object — two-device conflict?** → **Stop syncing the draft. Make it
device-local.** Ian: he would never finish a workout on another device. With that settled the draft
has no reason to cross the wire, and removing it from sync deletes a whole bug family — including the
crash fixed in `e98b957` and the endless draft-resurrection loop of 2026-07-25. Small and safe.

**3. What is the app for now?** → **"A workout tracker that keeps absorbing things."** Workouts are
the primary function; Ian also wants it helping with his daily routine and other areas. He asked
whether to strip features back. **Answer: no — and his own data settles it.** Over the 47 days to
2026-08-10: 38 weigh-ins, 31 journal entries, 27 workouts, 27 ideas, 25 hobby picks, 15 cardio, 12
lawn, 12 mobility, 7 todos, 6 pet weigh-ins. **Nothing is abandoned.** The problem was never feature
count; it is that each feature reinvented the sync rules and those rules live in memory. Ten features
with one written rulebook is fine. **"Keeps absorbing things" means thing eleven is coming, which is
exactly the case for F1.** Do not propose deleting features.

**4. Does the lawn scheduler earn its footprint?** → **Yes. Keep it.** 478 lines, ~12% of the file,
but 12 log entries in 47 days — roughly every four days, comparable to the workout cadence. It is in
genuine regular use, not an abandoned experiment. Its real cost is that `lawnLog` is a collection
whose merge rules are undeclared, which F1 fixes.

**5. What is localStorage actually at?** → **A non-issue. Closed.** Measured from Ian's own exports
(`ppl-backup-2026-06-24.json`, `ppl-backup-2026-08-10.json`). Note the export is pretty-printed, so
the file on disk is roughly double what the app actually stores; these are the compact figures:

| | |
|---|---|
| 2026-06-24 | 6.7 KB |
| 2026-08-10 | 66.8 KB |
| Growth | ~1.28 KB/day |
| Projected +1 year | ~0.5 MB |
| Projected +5 years | ~2.4 MB |
| Budget (shared with the 700 KB snapshot ring) | ~5 MB |

About **1.3% of budget**, on track to reach half of it around 2035. Do not spend effort here.

**6. Why email/password auth?** → **Keep email/password; close signup in the console.** Ian is doing
this himself (Firebase → Authentication → Settings → disable create/sign-up). Switching to Google
sign-in was considered and rejected: the data is keyed to the current auth uid, so a Google login
means a different uid and a hand migration of `users/{uid}` — real work and real risk, for a problem
one console toggle already solves.

### Two further decisions from the same conversation

**7. Export for Claude Desktop — build it.** Ian pulls his data into Claude Desktop to reason about
it. The current export is one raw JSON blob shaped for the *app*: sessions → entries → sets, nested,
with internal ids and `deletedAt` markers mixed in. Usable, but it burns effort on every conversation
before Claude can answer anything. **Add a second export producing a clean, readable file** —
workouts as flat rows (date, exercise, weight, reps), weigh-ins as a simple series, soft-deleted rows
and internal ids stripped. Schedule it to ride along with F1: once collections are declared in one
place, the clean export falls out of the same declaration rather than being separate work.

**8. Layout — leave it alone.** Workouts being "the main function" is a statement about emphasis, not
a complaint. Ian did not say the app feels wrong and nothing in the usage data suggests it does. No
navigation or hierarchy work. Revisit only if he raises it.

## Phase order — agreed 2026-09-09

Settled with Ian after the answers above. Plan against this.

**0. Turn off Firebase signup.** Console only, no code. Ian is doing it himself. Not a phase; listed
so nobody plans a code fix for it.

**1. F1 — the `COLLECTIONS` spec.** The one that pays off. Every collection's merge rule, soft-delete
behaviour, explicit-`false` requirement and sort order declared in one place, with `blank()`,
`mergeDB()`, the `liveX()` filters and `validateBackup()` derived from it. Retires the bug class
behind nearly every incident in `CLAUDE.md`. Justified by answer 3: the app keeps absorbing things.
**Constraint:** anything reached from `normalize()` runs at module-eval time and must not touch a
`const` declared further down — see the note at `index.html:638`.

**2. Export for Claude (answer 7).** Rides along with F1 — derive it from the same declaration rather
than hand-writing a second serialiser.

**3. F3 — the "adding a new tracked thing" recipe in `CLAUDE.md`.** Cheap, and it only reads well
once F1 exists to describe.

**4. Stop syncing the draft (answer 2).** Small and safe. Independent of the above; can land any time.

**5. F2 — event delegation.** Unlocks a CSP and permanently closes the escaping holes. Mechanical but
touches every handler; do it when the foundation above is settled.

**6. F4 — `.gitattributes`.** Its own commit, alone, never bundled — it makes `index.html` look
entirely rewritten.

**Explicitly NOT in scope**, each decided above rather than overlooked:

- Rewriting or removing cloud sync (answer 1)
- Deleting or trimming any feature, the lawn scheduler included (answers 3, 4)
- Anything about localStorage capacity (answer 5)
- Switching auth providers (answer 6)
- Navigation or layout changes (answer 8)
